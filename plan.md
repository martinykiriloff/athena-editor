# Athena vs VS Code — Gap Analysis & Implementation Plan

Audience: fullstack web developer on macOS. Strategy: **VS Code parity where it builds muscle memory, native speed as the moat.** This is not a plan to clone VS Code — it's a plan to close the gaps that break a web dev's inner loop (edit → save → see result), ranked by how often a user hits each gap per hour of work.

Snapshot of what Athena already has: regex syntax highlighting (13 languages incl. ISML/DS), Cmd+P quick open, find-in-file, go-to-line, toggle comment, minimap, inline git blame, ghost-text AI completion (Claude/Ollama), completion popup (Drizzle + LSP plumbing), git stage/commit/push/pull, ripgrep workspace search, DAP + CDP debugging (Node/Chrome/Next.js/Python/Swift), SwiftTerm terminal, npm script runner, Claude chat + Claude Code CLI panel, SFCC WebDAV sync + log tail, VS Code-parity keybindings (rebindable), auto-update.

---

## Part 1 — Gap list vs VS Code

Status legend: **Broken/dormant** = code exists but doesn't work · **Partial** = works with major limitations · **Missing** = not present.

### A. Language intelligence (worst area — highest impact)

| # | Feature | VS Code | Athena | Status |
|---|---|---|---|---|
| A1 | LSP server lifecycle | Automatic per language | `LSPManager.startServer` is **never called** — completions/hover dead in practice | **Broken/dormant** |
| A2 | Diagnostics (squiggles + Problems panel) | Live via LSP `publishDiagnostics` | `AppState.diagnostics` is never written; Problems panel always empty | **Broken/dormant** |
| A3 | Go to Definition / Peek | F12 / ⌥F12 | Cmd+Click resolves **import paths only** (`ImportResolver`), no symbol-level defs | Missing |
| A4 | Hover tooltips | Type info + docs on hover | `LSPManager.hover()` exists, no UI wired | **Broken/dormant** |
| A5 | Find All References | ⇧F12 | — | Missing |
| A6 | Rename Symbol | F2, cross-file | — | Missing |
| A7 | Format document / format-on-save | Built-in + Prettier | Settings flag exists, **no implementation** | Broken/dormant |
| A8 | Go to Symbol (⇧⌘O) / Outline view / breadcrumbs | Yes | — | Missing |
| A9 | Signature help, code actions / quick fixes | Yes | — | Missing |
| A10 | Semantic/tree-sitter highlighting | Semantic tokens | Regex re-run per keystroke (accuracy + perf ceiling on large files) | Partial |

### B. Core editing

| # | Feature | VS Code | Athena | Status |
|---|---|---|---|---|
| B1 | Multiple cursors / multi-select (⌥Click, ⌘D) | Yes | — | Missing |
| B2 | Auto-closing brackets/quotes + bracket-pair matching | Yes | — | Missing |
| B3 | Auto-indent on newline | Yes | Settings flag stored, not implemented | Broken/dormant |
| B4 | Find **& Replace** in file | ⌥⌘F | Find only (NSTextFinder bar) | Partial |
| B5 | Snippets with tab stops | Yes | Drizzle inserts are literal text | Missing |
| B6 | Code folding | Yes | — | Missing |
| B7 | Move/copy line up/down, duplicate line | ⌥↑/⌥↓ etc. | — | Missing |
| B8 | Sticky scroll | Yes | — | Missing |

### C. Workbench & navigation

| # | Feature | VS Code | Athena | Status |
|---|---|---|---|---|
| C1 | **Command palette** (⇧⌘P) | Core motion | Not implemented (Welcome screen even advertises it) | Missing |
| C2 | Split editors / editor groups | Yes | Single editor pane | Missing |
| C3 | Session restore (reopen tabs on launch) | Yes | Restores workspace only; **tabs lost** | Partial |
| C4 | External file-change detection | File watcher, auto-reload | **None** — no FS events; stale buffers possible | Missing |
| C5 | Tab drag-reorder; file-tree drag & drop | Yes | — | Missing |
| C6 | Search & Replace across workspace | Yes | Search only | Partial |
| C7 | Markdown preview; image preview | Yes | — | Missing |
| C8 | Zen mode / centered layout | Yes | — | Missing |
| C9 | Theme ecosystem | Thousands, importable | 3 fixed built-ins | Partial |

### D. Git

| # | Feature | VS Code | Athena | Status |
|---|---|---|---|---|
| D1 | Discard changes | Yes | **Bug: runs `git diff` and discards the output — file is never reverted** | Broken |
| D2 | Diff viewer (side-by-side/inline) | Core | Service can produce diffs; no viewer UI | Missing |
| D3 | Branch switcher UI (status-bar picker) | Yes | Service has branches/checkout; no UI | Missing |
| D4 | Gutter change indicators | Yes | — | Missing |
| D5 | Merge-conflict resolution UI | Yes | — | Missing |
| D6 | Stash, commit-history browser, clone repo | Yes | Clone is a Welcome-screen placeholder; no stash; `log` unused by UI | Missing |

### E. Terminal, tasks, debug

| # | Feature | VS Code | Athena | Status |
|---|---|---|---|---|
| E1 | Multiple terminals / tabs / split | Yes | Single terminal instance | Partial |
| E2 | Watch expressions, conditional breakpoints, logpoints | Yes | Plain breakpoints only | Partial |
| E3 | Debug console REPL (evaluate in frame) | Yes | Output view only, no input | Partial |
| E4 | Hover-to-inspect variables while paused | Yes | — | Missing |
| E5 | tasks.json / generic task runner | Yes | npm scripts only (fine for target user; low priority) | Partial |

### F. Deliberate non-goals (don't build)

- **Extension marketplace** — Athena's bet is native, integrated, fast. Ship the top extensions' *functionality* built-in (Prettier, GitLens-style blame ✓ already, themes) instead of an extension host.
- **Remote development / SSH / containers, Live Share, notebooks, Settings Sync, profiles, web version.** Wrong fights for a native single-window macOS IDE.
- Athena-specific surfaces VS Code lacks (SFCC sync/logs, Claude panels, DB connection manager) are differentiators — out of scope here, except one note: the DB panel has **no query execution** (GRDB is an unused dependency). Either ship a minimal query runner or hide the panel until it's real; a connect toggle that does nothing erodes trust.

---

## Part 2 — Implementation plan

Ordered by inner-loop impact ÷ effort. Each phase is shippable on its own. Effort: S ≤ 1 day, M ≈ 2–4 days, L ≈ 1–2 weeks.

### Phase 0 — Fix what's already built but dead (1 week, mostly S) — ✅ DONE (2026-07-04)

The highest-ROI work in the repo: three subsystems are ~80% built and 0% functional.

1. ✅ **Wire the LSP lifecycle (A1)** — M
   - In `AppState`, on workspace open / first file of a language: `await lspManager.startServer(for: language, workspaceRoot:)`. Send `textDocument/didOpen` on tab open, `didChange` on edit (already exists, never called), `didClose`/`shutdown` on teardown.
   - Store server tasks as `@ObservationIgnored` tasks per the concurrency contract; cancel on workspace close.
   - Smallest slice: TS/JS only (`typescript-language-server`), completions actually appearing. Then flip on pylsp/gopls/rust-analyzer/sourcekit-lsp — the launch code already exists.
2. ✅ **Diagnostics pipeline (A2)** — M
   - Handle `textDocument/publishDiagnostics` notifications in `LSPManager`, expose an `AsyncStream<[Diagnostic]>`, consume it in `AppState.diagnostics`. Problems panel and status-bar counters light up with **zero UI work** — they're already built.
3. ✅ **Fix Git "Discard Changes" (D1)** — S
   - Replace the `diff` call with `git restore --worktree --` (or `checkout --` pre-2.23). Add a confirmation dialog — it's destructive. Add `restore` to `GitService`.
4. ✅ **Honor dead settings or remove them (A7, B3)** — S for removal decision; implementation lands in Phases 1–2 below. Ship auto-indent-on-newline now (S): on `insertNewline`, copy previous line's leading whitespace; +1 indent level after `{`, `(`, `[`, `:`.

**Exit criteria:** typing in a `.ts` file shows real LSP completions; a type error shows in Problems + status bar; Discard Changes actually reverts.

**Implementation notes:**
- `LSPManager`: added `didOpen`/`didClose`/`stopAllServers`/`diagnosticsStream()`; `readLoop()` now branches on JSON-RPC notifications (no `id`) vs request responses, parsing `textDocument/publishDiagnostics` (LSP severity 1–4 → `DiagnosticSeverity`, 0-based line/character → this app's 1-based convention).
- `AppState`: `openFile` starts the server + sends `didOpen`; `updateTabContent` fires `didChange`; `closeTab` sends `didClose` + clears that file's diagnostics; `openWorkspace`/`closeFolder` **await** `lspManager.stopAllServers()` before proceeding (a first-pass version used an unawaited `Task { }` here, which the verify pass caught as a stale-server-across-workspace-switch race — fixed by awaiting inline and converting `closeFolder()` to `async`, with its `MainWindowView.swift` call site updated to `Task { await appState.closeFolder() }`). Added `discardChanges(path:)`: real `GitService.restore` (`git checkout -- <path>`), refreshes git status, and reloads any open tab for that file directly from disk (bypassing `updateTabContent` so it doesn't re-mark dirty or re-fire `didChange`).
- `GitPanelView`: "Discard Changes" now shows a confirmation alert (matching `FileTreeView`'s delete-confirmation pattern) before calling `appState.discardChanges(path:)`.
- `EditorView`: `autoIndent` threaded through from `appState.editorAutoIndent`; Return key copies leading whitespace and adds one indent unit after `{`/`(`/`[`/`:`, respecting tab size / spaces-vs-tabs.
- Side fix: `make test` was failing in this dev environment with "no such module 'Testing'" (Xcode Command Line Tools only, no full Xcode.app). Root-caused and fixed in the `Makefile` (detects CLT-only installs and passes the right `-F`/`-rpath` flags to `swift test` so swift-testing resolves) rather than left as an accepted gap.
- Verified independently (build + all 22 tests) after the workflow completed — both green.
- Known minor rough edges intentionally left for later phases: `didChange` fires unthrottled on every keystroke (no debounce/coalescing — a perf nit, not a correctness bug); auto-indent only inspects the single character immediately before the cursor (doesn't skip trailing whitespace); "Discard Changes" is still offered for untracked ("?") files where `git checkout --` harmlessly no-ops instead of deleting.

### Phase 1 — Muscle-memory parity (2–3 weeks)

5. ✅ **Command palette ⇧⌘P (C1)** — M — DONE (2026-07-04)
   - Reuse `QuickOpenView`'s window/list/ranking; back it with a command registry derived from the existing `KeyBinding` table + `AppState.perform(_:)` actions so every command shows its keybinding. `>` prefix in Cmd+P switches modes, matching VS Code.

   **Implementation notes:**
   - `KeyBinding.swift`: added `KeyAction.commandPalette` (`"workbench.action.showCommands"`), display name "Command Palette", grouped into the existing "Navigation" category alongside `.quickOpen`; added its VS Code default `KeyBinding(action: .commandPalette, combo: KeyCombo(key: "p", command: true, shift: true))` (⇧⌘P) to `KeyBinding.vscodeDefaults`. No new `SharedTypes.swift` type needed — reused `KeyBinding`/`KeyCombo` as-is.
   - `AppState.swift`: extended the existing quick-open state instead of adding a parallel `showCommandPalette` bool — added `var quickOpenPrefill: String = ""`, and `presentQuickOpen(prefill:)` now seeds it ("" for `.quickOpen`, ">" for `.commandPalette`) before setting `showQuickOpen = true`. `perform(_:)` gained `case .commandPalette: presentQuickOpen(prefill: ">")`; dispatch reaches it through the same `dispatchKeyInfo` → `perform` path already used by every other binding (no key-monitor changes needed).
   - `QuickOpenView.swift`: made mode-aware rather than adding a sibling view, so the two palettes share one visual system. `isCommandMode` = `query.hasPrefix(">")`; `.onAppear` seeds local `@State query` from `appState.quickOpenPrefill`. In command mode the list source switches to `appState.keyBindings` (the **effective**, override-aware bindings loaded by `KeyBindingService`/`installKeyMonitor`, not the raw `vscodeDefaults`) filtered by substring match against `action.displayName` with prefix-match ranked first (mirrors the existing file-search ranking). Added `CommandPaletteRow` (same padding/selection-highlight chrome as `QuickOpenRow`) showing displayName + category + the binding's `combo.displayString` (blank if unbound). Return invokes `Task { await appState.perform(action) }` then closes, matching how the key monitor itself dispatches. Arrow-key nav/auto-scroll/Escape all reused via a shared `currentResultCount`/`move`/`confirmSelection` that branch on `isCommandMode`.
   - `AthenaCommands.swift`: added a "Command Palette…" item to the Go menu (`performAction(.commandPalette)`), matching the existing "Go to File…" wiring pattern, for menu-bar discoverability.
   - Welcome screen's "⇧⌘P Command Palette" cheat-sheet entry now does something.
   - Verified: `make build-debug` zero warnings/errors; `make test` — all 22 tests still pass (no new tests added — this is a UI-wiring change over existing, already-tested `perform(_:)`/`KeyBindingService` logic, not new business logic).
6. ✅ **File watching (C4)** — M — DONE (2026-07-05)
   - New `actor FileWatchService` on `FSEventStream` (or `DispatchSource` per open file). Auto-refresh file tree; for open tabs: silently reload if not dirty, show "file changed on disk" bar if dirty. This prevents silent data loss — arguably Phase 0 severity.

   **Implementation notes:**
   - `FileWatchService.swift` (new): per-open-file + workspace-root `DispatchSource.makeFileSystemObjectSource` watching (`O_EVTONLY` fd, `.write`/`.delete`/`.rename` for files, `.write` for the root directory) — no new dependency, pure Foundation/Dispatch, per the task's scoped-down alternative to a full FSEvents tree watch. Single-subscriber `eventStream() -> AsyncStream<FileWatchEvent>` mirrors `LSPManager.diagnosticsStream()`'s continuation-storage pattern exactly. Event handlers capture only `[weak self]` + the `Sendable` `URL` (not the non-`Sendable` `DispatchSourceFileSystemObject`) and look the watch back up inside the actor to read `.data`, avoiding any actor-boundary Sendability issue.
   - `SharedTypes.swift`: added `FileWatchEvent` enum (`.fileChanged`/`.fileDeleted`/`.directoryChanged`, both `Sendable` + `Equatable`) and `TabModel.externallyModified: Bool`.
   - `AppState`: `fileWatchService` added to the service list (constructor-injectable, default `FileWatchService()`); `startFileWatchConsumer()` (called once from `AthenaApp.swift` alongside `startDiagnosticsConsumer()`) subscribes for the app's lifetime. Lifecycle wiring mirrors `LSPManager`'s per-language servers: `openFile` → `watchFile`, `closeTab` → `stopWatchingFile`, `openWorkspace` → `watchDirectory` (replaces any prior root watch), `closeFolder` → `stopAll()`; `saveActiveTabAs` re-points the watch from the old path to the new one so repeated Save-As doesn't leak descriptors.
   - Directory events debounce 300 ms (`fileTreeRebuildTask`, cancel-and-reschedule) before calling `fileService.buildFileTree` again, so a burst (git pull, `npm install`) causes one rebuild, not one per touched file — matches the existing full-tree-replace behavior `FileTreeView` already does after its own create/delete/rename actions (expansion state isn't preserved today either way).
   - File-change events: if the tab isn't dirty, reload its content directly from disk (`fileService.readFile`, set `content`/`isDirty` directly — the exact same bypass-`updateTabContent` pattern `discardChanges(path:)` already uses, so it doesn't re-mark dirty or re-fire LSP `didChange`); if the tab is dirty, set `TabModel.externallyModified = true` instead of clobbering it.
   - Delete/rename events are disambiguated in `AppState` rather than trusted at face value: `DispatchSource` reports "gone" identically for a real delete and for an atomic-save tool (`git checkout`, many editors) that writes a temp file and renames it over the original — indistinguishable at the fd level once the descriptor stops resolving. `handleExternalFileRemoval` checks whether the path still exists; if it does, it re-arms the watch (the service already tore down the stale one) and treats it as an ordinary content change instead of a false "deleted" banner. Only a genuinely missing path calls `markFileOrphaned`, which forces the tab dirty and posts a `statusMessage` ("saved to recreate it") per the task's "status message is sufficient signaling for this pass" scope.
   - UI: `EditorContainerView.swift` gained `ExternalChangeBanner` — a warning-tinted bar (same `HStack`/height/`.overlay(alignment: .bottom) { Divider() }` chrome as `DebugToolbarView`) shown above the editor when the active tab's `externallyModified` is set, with "Reload" (`appState.reloadTabFromDisk`) and "Keep My Changes" (`appState.keepLocalChanges`) actions.
   - Every other path that resyncs a tab with disk (`saveActiveTab`, `saveAllTabs`, `saveActiveTabAs`, `revertActiveTab`, `discardChanges`) also clears `externallyModified` now, so a normal save/revert/discard doesn't leave a stale banner showing.
   - New test suite `FileWatchServiceTests` (4 tests: watched-file write → `.fileChanged`, watched-file delete → `.fileDeleted`, `stopWatchingFile` silences further events, watched-directory entry → `.directoryChanged`) using a polling `waitUntil` helper (bounded, non-flaky) instead of a fixed `sleep`; total suite now 26 tests, all green, verified stable across repeated runs.

   **Flakiness investigation (2026-07-06):** this suite became visibly flaky once later Phase 2 work drove the total suite past ~50-80 tests (`make test` failing roughly 1 run in 3-5). Root-caused two real issues and fixed both, though a residual environment-dependent flake remains:
   - **Real bug, fixed:** `eventStream()` (and `LSPManager.diagnosticsStream()` / `SearchService.search()`, same pattern) stored their `AsyncStream.Continuation` via a *nested* `Task { self.storeXxx(continuation) }` inside the `AsyncStream { continuation in ... }` builder closure. That nested Task races the producer: if a `DispatchSource` event (or an LSP `publishDiagnostics` notification) fires before the nested Task gets scheduled, the continuation is still `nil` and the event is dropped forever — not delayed, *lost*. This is a genuine correctness bug beyond the tests: it could silently drop a file's first diagnostics batch or the first file-change event in production. Fixed all three call sites with `AsyncStream.makeStream()`, which yields the continuation as a plain value so it's stored via an ordinary synchronous actor-isolated assignment — no nested Task, no race.
   - **Real improvement, applied:** `makeWatch`'s `DispatchSource` ran on `DispatchQueue.global(qos: .utility)` — a QoS macOS will deprioritize behind ordinary work. Bumped to `.userInitiated`, since this watch's entire purpose is *timely* external-change detection.
   - **Residual, NOT fully resolved:** even after both fixes, `.serialized` on the suite, and raising the polling timeout from 3s to 10s, `watchedFileWriteYieldsFileChanged` (and occasionally the delete/directory tests) still fail intermittently — 8 of 15 `make test` runs in one measured batch — specifically when run as part of the full ~80-test suite; the identical test is 100% reliable across dozens of runs in isolation (`swift test --filter FileWatchServiceTests`). A longer timeout does not help (rules out "just slow"), which means the underlying kqueue event is occasionally not delivered at all within a generous window when this process is running ~75 other concurrent tests — most likely contention in the kernel's file-event delivery path from many tests doing concurrent real filesystem I/O in `FileManager.default.temporaryDirectory`, though this wasn't conclusively isolated further given the time already spent. This is a test-environment limitation of exercising a real `DispatchSource`/kqueue integration inside a large concurrent test binary, not a flaw in the shipped file-watching behavior itself (which the isolated 100%-reliable runs and the code-level race fix both support). Documented rather than chased further; a future pass could split `FileWatchServiceTests` into its own test target/process to remove the shared-process contention entirely.
7. ✅ **Find & Replace, in-file and workspace (B4, C6)** — M — DONE (2026-07-05)
   - In-file: custom find bar (NSTextFinder replace is clunky) with replace/replace-all, regex, case, whole-word.
   - Workspace: add replace field to `SearchPanelView`; preview per-match, apply via `FileService` batched writes. Ripgrep already yields file/line/range.

   **Implementation notes:**
   - `SharedTypes.swift`: added `TextSearchMatcher` (in the existing "Search" section, next to `SearchResult`) — a single `Sendable` literal/regex match-and-replace engine (`matches(in:)`, `replacementText(forMatchIn:range:replacement:)` honoring `$1`-style regex capture groups, `replacingAll(in:with:)` applying back-to-front so earlier ranges stay valid) shared by both the in-file bar and the workspace replace, so case-sensitivity/whole-word/regex semantics can't drift between the two surfaces. Whole-word wraps the (escaped, if literal) pattern in `\b(?:…)\b`; literal-mode replacement text is run through `NSRegularExpression.escapedTemplate(for:)` so a literal `$1` in the replacement field is never misread as a capture reference. Also added `EditorCommand.findAndReplace`.
   - `KeyBinding.swift`: added `KeyAction.findAndReplace` (`"editor.action.startFindReplaceAction"`, VS Code's own command id), category "Editor", default binding ⌥⌘F (`KeyCombo(key: "f", command: true, option: true)`) alongside the existing ⌘F `.findInFile`.
   - `AppState.perform(_:)`: `.findAndReplace` forwards to `postEditorCommand(.findAndReplace)`, identical dispatch path to `.findInFile`. New `replaceAllInWorkspace(query:replacement:regex:caseSensitive:wholeWord:filter:)` (new "Workspace Search & Replace" section): re-runs `SearchService.search` itself (rather than trusting the panel's possibly-stale `searchResults`) to get the current file list, applies `TextSearchMatcher.replacingAll` per file, writes changed files via `fileService.writeFile` (never ripgrep's own `--replace`, so results stay consistent with how the rest of the app writes files), and — for any file that's also an open tab — sets `content`/`isDirty = false`/`externallyModified = false` directly, the same bypass-`updateTabContent` pattern `discardChanges(path:)` and the FileWatchService reload path already use. Publishes a `statusMessage` summary ("Replaced N occurrences across M files" / "No occurrences of … found") since a workspace-wide bulk replace is destructive enough to want visible confirmation, without a blocking modal.
   - `Editor/FindReplaceController.swift` (new): `@Observable @MainActor final class` owning the in-file bar's state (query/replacement/regex/case/whole-word toggles, `matches: [NSRange]`, `currentMatchIndex`, `isVisible`/`showReplace`) and a `weak var textView: NSTextView?`, created and wired in `EditorView.makeNSView` exactly like the existing `EditorScrollProxy` (constructed once, handed out via a `Binding<FindReplaceController?>?` parameter, and also stashed on the `Coordinator` for internal notification handling). Matches are highlighted via `NSLayoutManager.addTemporaryAttribute(.backgroundColor, …)` (current match `NSColor.findHighlightColor`, others at 35% alpha) rather than `NSTextStorage` attributes, so highlighting can't be clobbered by — or clobber — `SyntaxHighlighter`'s per-keystroke `setAttributedString` and never touches the undo stack. `replaceCurrent()`/`replaceAll()` go through the identical `shouldChangeText`/`replaceCharacters`/`didChangeText` undo-aware path `EditorView.Coordinator.replace(_:with:in:)` already uses, so edits group into undo correctly and fire `textDidChange` (keeping the bound tab content and highlighting in sync). Deliberately uses plain stored properties (no `didSet`) with the SwiftUI view calling `updateMatches()` explicitly on every change — mirrors `SearchPanelView`'s existing `.onChange(of:) { … }` pattern rather than relying on `@Observable` + property-observer interaction, which has no precedent elsewhere in the codebase.
   - `EditorView.swift`: replaced the `NSTextFinder`-based `showFindBar` (`tv.performTextFinderAction(.showFindInterface)`) entirely — `.find`/`.findAndReplace` now call `findReplaceController?.present(withReplace:)`. `Coordinator.handle(_:)` no longer requires the text view to be `firstResponder` for find commands specifically (only for `.goToLine`/`.toggleComment`/`.indent`/`.outdent`), since ⌘F/⌥⌘F must still reach the editor even when focus is already inside the bar's own search field or in the terminal, matching VS Code. Escape (`handleKeyDown` case 53) now also dismisses the bar (after completion popup / ghost text, in that priority order).
   - `UI/FindReplaceBarView.swift` (new): floating SwiftUI bar (`@Bindable var controller: FindReplaceController`), overlaid `.topTrailing` on the editor in `EditorContainerView.CodeEditorView` (a new `@State private var findReplaceController: FindReplaceController?` threaded through the same `Binding` pattern as `scrollProxy`). Toggle buttons for regex/case/whole-word reuse `SearchPanelView`'s existing visual language (monospaced glyph, accent-tinted when on) for consistency. A `focusRequestToken` on the controller (bumped every `present` call) lets the bar refocus its query field even when re-invoked while already visible (e.g. ⌥⌘F pressed again while the replace field has focus) — `onAppear` alone only fires once per mount.
   - `AthenaCommands.swift`: added a "Find" menu (`Find…` / `Find and Replace…`) mirroring the Go menu's `performAction(...)` wiring, for menu-bar discoverability — no such menu existed before this.
   - `SearchPanelView.swift`: added a disclosure chevron (left of the magnifying glass, VS Code's placement) toggling a new replace row (replacement field + "Replace All" button, disabled when there are no current results) below the search bar. "Replace All" calls the new `AppState.replaceAllInWorkspace` with the panel's existing regex/case toggles and include/exclude filter, then re-runs `triggerSearch()` so the results list reflects the new file contents; the button's tooltip states the exact scope ("Replace N occurrences across M files") as the "before" visibility the task asked for, with the `statusMessage` summary serving as the "after".
   - New test suite `TextSearchMatcherTests` (12 tests: case-insensitive/case-sensitive literal matching, whole-word boundary exclusion, regex special chars treated literally in non-regex mode, regex pattern matching, invalid-regex and empty-query init failures, `replacingAll` counts/no-op/capture-group templating, literal-mode replacement never treating `$1` as a template, single-match `replacementText` capture groups) — total suite now 38 tests. All green on repeated `make build-debug` (zero warnings) + `make test` runs; one pre-existing, unrelated flake was observed in `FileWatchServiceTests` (Phase 1 item 6, untouched by this change) on ~2 of 8 `make test` invocations during this work — a DispatchSource-timing sensitivity worth a separate look, not introduced here.
   - Verify pass: build/tests green throughout; could not obtain an on-screen screenshot of the bar in this sandbox (`screencapture`/`osascript System Events` both denied by TCC — Screen Recording/Accessibility not authorized for this shell's execution context, confirmed via a read-only TCC.db probe that was itself denied). The app process does launch and stay resident under the real binary path (`.build/debug/Contents/MacOS/Athena` — note `make run`'s `.build/debug/$(APP_NAME)` path is currently a directory, not the executable, on this machine; a separate Makefile nit, not fixed here). Manual on-screen confirmation of the bar's rendering/interaction is still outstanding.
8. **Auto-closing brackets + pair highlight (B2)** — S/M — `AthenaTextView.insertText` interception; skip-over on typing the closing char; highlight matching bracket at caret.
9. ✅ **Session restore (C3)** — S — DONE (2026-07-05)
   - Persist open tabs + active tab + cursor per workspace via `SettingsService` JSON keyed by workspace path; restore in `restoreLastWorkspace`.

   **Implementation notes:**
   - `SharedTypes.swift`: added `SessionTab` (`path`, `cursorLine`, `cursorColumn`) and `WorkspaceSession` (`tabs: [SessionTab]`, `activePath: String?`), both `Codable & Sendable`, in a new "Session Restore" section after `TabModel`.
   - `AppState.swift`: new private `persistSession()` reads the existing `[String: WorkspaceSession]` dict from the `"workspaceSessions"` settings key (via the same generic `settingsService.value(for:default:)`/`setValue` JSON API `lastWorkspacePath` already uses), overwrites the entry for the *current* workspace's `rootURL.path` with the current `openTabs` (file-backed tabs only — untitled tabs have no `fileURL` and are intentionally skipped) + each tab's `cursorLine`/`cursorColumn` + the active tab's path, and writes the whole dict back — so two different project folders each keep their own tab layout in one file rather than a single global list. Called (fire-and-forget `Task` where the call site isn't already `async`) from every tab-affecting mutation: `openFile` (both the "already open, just activate" branch and the newly-opened branch), `closeTab`, the new `activateTab(_:)`, `openNewTab`, and `cycleTab`.
   - New private `restoreSession(for:)` looks up the workspace's persisted `WorkspaceSession`, replays `openFile(_:)` for each tab **in persisted order** (so LSP `didOpen`/blame-loading/file-watch registration all still fire normally), skipping — via a plain `FileManager.fileExists` check — any path that no longer exists on disk (silently, no error/crash), then copies the persisted `cursorLine`/`cursorColumn` onto the resulting `TabModel` (restoring the caret's *line/column value* on the model; actually scrolling the view to it is left as the follow-up nicety the task explicitly allowed skipping), restores `activeTabId` from the persisted active path, and finally re-calls `persistSession()` once more so a since-deleted file's entry is dropped from the saved session immediately rather than being retried on every future launch.
   - Wired into both restore paths the task asked for: `openWorkspace(_:)` now calls `await restoreSession(for: url)` after `discoverNPMPackages()`, so it fires both for `restoreLastWorkspace()` (launch) and for an explicit "Open Folder" pick of a folder that already has a saved session.
   - Added `AppState.activateTab(_:)` (sets `activeTabId` then persists) and routed `TabBarView`'s tab-click `.onTapGesture` through it instead of setting `appState.activeTabId` directly from the view; also routed `TabBarView`'s "+" button and `WelcomeView.newFile()` through the existing `AppState.openNewTab()` instead of each duplicating the same untitled-tab-creation + `activeTabId` assignment inline — both changes needed so persistence fires from every activation/creation site without scattering `Task { await persistSession() }` calls into UI files.
   - Also cleaned up `restoreLastWorkspace`'s stray placement under the unrelated `// MARK: - AI Ghost Text` section into a proper new `// MARK: - Session Restore` section housing it alongside the two new methods.
   - Verified: `make build-debug` — zero warnings/errors. `make test` — all 38 existing tests still pass (no new tests added; this is state-persistence wiring over already-tested `openFile`/`closeTab` behavior, not new business logic in an isolated unit).
10. ✅ **Go to Definition + hover UI (A3, A4)** — M — DONE (2026-07-05)
    - `textDocument/definition` in `LSPManager`; route Cmd+Click through LSP first, `ImportResolver` as fallback. Hover: floating NSPanel after 500 ms dwell, rendering the already-parsed hover contents.

    **Implementation notes:**
    - `LSPManager.swift`: added `definition(fileURL:line:character:)` — same `sendRequest`/param shape as `complete`/`hover`. New `parseDefinition(from:)`/`location(from:)` handle the response `result` as a single `Location` (`{uri, range}`), an array of `Location`, or an array of `LocationLink` (`{targetUri, targetRange}`) — taking the first entry when several come back, and converting the 0-based LSP position to this app's 1-based convention (mirrors `parseDiagnostics`'s existing conversion).
    - `SharedTypes.swift`: added `DefinitionLocation` (`fileURL`, 1-based `line`/`character`), `Sendable`/`Equatable`, next to `CompletionItem`.
    - Cmd+Click order is import-first, not LSP-first as the one-liner above originally sketched: `EditorView.makeNSView`'s `onCmdClick` still checks `Coordinator.importPath(from:at:)` first (no round-trip, instant), and only calls the new `Coordinator.requestDefinition(at:)` when that returns nil — cheaper path stays cheapest; this deviation from the plan bullet was intentional per the task brief and is a better default than the reverse.
    - `EditorView.swift`: `requestDefinition(at:)` converts the clicked char index to 1-based line/col via a new `position(in:at:)` helper (refactored out of the existing `cursorPosition(in:)`, which now just calls it with the current selection), then awaits a new `onRequestDefinition` closure. Same-file results scroll immediately via a new `scrollToPosition(line:column:in:)` (mirrors `promptGoToLine`'s line-walking, extended to land on a column) called directly on the Coordinator's own `textView` — simpler and more direct than routing through `EditorScrollProxy`, which only supports fraction-based scrolling and can't target a line/column. Cross-file results call a new `onOpenDefinitionFile` closure (→ `AppState.openFile`) and stash the target in `pendingDefinitionScroll`; `updateNSView` calls the new `consumePendingDefinitionScroll(in:)` right after it loads a newly-activated tab's content into the (reused) text view, completing the jump once the target file's content has actually landed — avoids scrolling against stale/mismatched content from the previous tab.
    - `EditorContainerView.swift` (`CodeEditorView`): wires `onRequestDefinition`/`onRequestHover` to `appState.lspManager.definition`/`.hover` (1-based → 0-based at the call site, same convention `onRequestCompletion` already uses) and `onOpenDefinitionFile` to `appState.openFile`.
    - Hover UI: new `HoverWindowController` (new file, `Sources/Athena/Editor/HoverWindowController.swift`) — a non-activating `NSPanel` copying `CompletionWindowController`'s exact panel chrome (`.nonactivatingPanel`/`.borderless`, `.popUpMenu` level, `NSVisualEffectView` frosted background), sized to the hovered text via manual `NSString.boundingRect` measurement (no Auto Layout needed for a single wrapping label) instead of a table.
    - `AthenaTextView.swift`: added `onHoverMove`/`onHoverExit` closures fed from `mouseMoved`/`flagsChanged` (new `updateHoverTooltipTracking`, mirroring the existing `updateHoverCursor` structure) and from `mouseExited`. Suppressed whenever Cmd is held, so Cmd-hover stays exclusively "clickable target" (pointing-hand cursor) rather than also popping a tooltip.
    - `EditorView.swift` `Coordinator`: `scheduleHover(at:)` debounces 500 ms (same `Task.sleep` + cancel-and-reschedule style as `completionDebounce`/`ghostDebounce`), ignores repeat calls for the same character, and calls the new `onRequestHover` closure; `triggerHover` positions the panel via `NSTextView.firstRect(forCharacterRange:actualRange:)`, the same API `triggerCompletion` already uses for the completion popup's anchor. Dismissal wired at all three requested triggers: mouse-move-away (`onHoverExit` → `cancelHover()`), any keystroke (unconditional `cancelHover()` at the top of `handleKeyDown`, which also covers Escape), and `textDidChange` (alongside the existing ghost-text dismissal).
    - Verified: `make build-debug` — zero warnings/errors on a full clean rebuild. `make test` — all 38 existing tests still pass (no new tests added: `LSPManager` has no existing test coverage to extend — its actor methods all require a live subprocess language server, which nothing in this suite spins up — so the new `definition`/parsing code follows that same untested-by-design precedent rather than introducing a one-off inconsistent test harness).
    - Not independently verified in this pass: on-screen confirmation of the hover panel's rendering and the cross-file scroll-to-position behavior against a *real* running language server (this dev environment's LSP executables — see `LSPManager.executablePath(for:)` — may not all be installed); the logic was verified by code inspection and the green build/test run, matching the precedent already set by item 7's note for its own unverified on-screen rendering.

**Exit criteria:** a VS Code user's hands work unchanged: ⇧⌘P, ⌘P, ⌥⌘F, F12/Cmd+Click, and no stale-file surprises.

### Phase 2 — Editing power (3–4 weeks)

11. **Multiple cursors (B1)** — L — `NSTextView` supports `selectedRanges`; implement ⌥Click add-caret, ⌘D select-next-occurrence, ⇧⌥drag column select, multi-range typing/paste. Hardest single item in the plan; ship ⌘D first (80% of usage).
    - **Done (scoped):** ⌘D (Select Next Occurrence) and ⌥Click add-cursor shipped; ⇧⌥drag column select was **not** attempted (out of scope for this pass). `KeyAction.selectNextOccurrence` (`editor.action.addSelectionToNextFindMatch`) bound to ⌘D in `KeyBinding.vscodeDefaults`, dispatched via `AppState.perform` → `EditorCommand.selectNextOccurrence` → `EditorView.Coordinator.selectNextOccurrence(_:)`. No selection → selects the word touching the caret (handles caret at EOF / word touching file boundaries). With a selection → finds the next case-sensitive, wrapping occurrence of the last range's exact text and appends it to `selectedRanges`; no more occurrences → no-op. Typing then replaces every selected range via AppKit's *native* multi-range `insertText`/Backspace/Return — no hand-rolled multi-caret typing was written.
      Found and fixed a real bug while verifying that native path: `EditorView.Coordinator.applyHighlighting(to:)` re-syntax-highlights after *every* keystroke (per this app's regex-highlighter design) and was reading/restoring only the singular `textView.selectedRange()` — it would have silently collapsed any multi-range selection back to one range right after the first keystroke, breaking multi-range typing entirely. Fixed to preserve/restore the full `selectedRanges` array. Also audited every other `shouldChangeText`/selection-reading call site that could see multiple ranges: `handleInsertText` (bracket/quote auto-close/wrap) and `deleteEmptyPairIfPresent` (Backspace pair-delete) now explicitly bail out (`return false`) when `selectedRanges.count > 1`, deferring to AppKit's native multi-range handling instead of silently acting on just the primary range and dropping the rest; same for the Return-key auto-indent path (multi-caret Return now inserts a plain "\n" at each range via the native path rather than computing indent for one range only).
      ⌥Click (not ⌘Click, to avoid the existing Phase 1 Cmd+Click go-to-definition/import-nav binding) adds an empty caret at the click point via a new `AthenaTextView.onOptionClick` hook; deliberately skips `super.mouseDown` for that branch so the default click doesn't collapse `selectedRanges` first. Escape collapses a multi-range selection to a single cursor (the highest-location range) as the last step of the existing Escape dispatch chain in `handleKeyDown` (after completion popup / ghost text / find bar dismissal), gated on `selectedRanges.count > 1`.
      Not done: column/box selection (⇧⌥drag), and no dedicated automated tests were added for the new Coordinator logic (it's private, deeply tied to `NSTextView`, no existing unit-test seam for it — verification was build + full 38-test suite green + careful manual/logical edge-case reasoning, not interactive GUI testing). `make build-debug` is warning/error-free under Swift 6 strict concurrency; `make test` is 38/38 green.
12. **Format-on-save (A7)** — M — LSP `textDocument/formatting` first; fall back to project-local Prettier (`node_modules/.bin/prettier`) via `Process` for the web languages — reads the project's own config, matching "meet projects where they are."
    - **Done (2026-07-05):** `LSPManager.formatting(fileURL:content:tabSize:insertSpaces:)` sends `textDocument/formatting` (`options: {tabSize, insertSpaces}`), parses the `TextEdit[]` response, and applies it via a new pure/testable top-level `applyLSPTextEdits(_:to:)` — converts each edit's 0-based `(line, character)` LSP position to a UTF-16 offset against the *original* content, then applies edits back-to-front (sorted by start offset descending) so one edit's replacement never invalidates another's still-pending offset. Returns `nil` when the server isn't running or reports no edits.
      New `PrettierService` actor (mirrors `GitService.run(args:at:)`'s `Process`/`Pipe`/`withCheckedThrowingContinuation` shape): checks for `<workspaceRoot>/node_modules/.bin/prettier` and only runs it if present (never a global install), running `prettier --stdin-filepath <path>` and draining stdout/stderr concurrently via readability handlers (writing stdin off-actor on a background queue) so a file larger than the stdin pipe's OS buffer can't deadlock against Prettier's own stdout filling up. Silently returns `nil` (no error) when no local binary exists.
      `AppState.formattedContent(for:)` is the single new decision point: format-on-save on + has a file URL → try LSP first, then (only for `.typescript/.javascript/.css/.html/.json/.markdown`, and only with a workspace open) Prettier. Wired into both `saveActiveTab()` (Cmd+S / Save menu) and `saveAllTabs()` (Save All), updating `openTabs[index].content` before the disk write so the editor visibly shows the formatted result (same "just mutate `openTabs[index].content`" pattern already used by `discardChanges`/`revertActiveTab`/silent-reload — `EditorView.updateNSView` diffs `textView.string` against the bound `content` and refreshes it).
      Added `@Suite("applyLSPTextEdits")` (6 tests: empty edits, single replacement, multiple same-line edits applied in descending order, multi-line span, deletion, whole-document replacement) — the only piece of this feature with a pure-function seam; the Process-based paths (real LSP server, real Prettier binary) are exercised by build + manual reasoning only, no test harness for spawning a fake LSP server existed to reuse. `make build-debug` warning/error-free under Swift 6 strict concurrency; `make test` 44/44 green (38 prior + 6 new).
13. **Rename symbol + references (A5, A6)** — M — LSP `rename` returns a `WorkspaceEdit`; apply across open/closed files via `FileService`; references list reuses search-results UI.
    - **Done (2026-07-05):** `LSPManager.references(fileURL:line:character:includeDeclaration:)` sends `textDocument/references` and reuses the existing `location(from:)` `Location`-parsing helper (already shared with `definition`) — no duplicate parsing added. `LSPManager.rename(fileURL:line:character:newName:)` sends `textDocument/rename` and parses the `WorkspaceEdit` result (`changes` map, plus the alternate `documentChanges` form) into `[URL: [LSPTextEdit]]`; extracted the inline `TextEdit`-JSON-parsing `formatting` used to a shared `private static func parseTextEdit`, now used by both `parseFormatting` and the new `parseWorkspaceEdit` so the JSON shape is decoded once. Callers apply each file's edits with the existing `applyLSPTextEdits` — not duplicated.
    - `KeyBinding.swift`: added `KeyAction.renameSymbol` (F2, plain, per VS Code, as directed) and `KeyAction.findReferences`. **Scope deviation:** Find All References is bound to **⌥⇧F12**, not VS Code's plain ⇧F12 — on stock Mac keyboard settings ("Use F1, F2, etc. keys as standard function keys" off, the default), F12 alone is the volume-up media key and never reaches the app as a keyDown at all, so plain ⇧F12 would silently never fire for most users out of the box. ⌥⇧F12 avoids that collision; documented inline at the binding site. (F2 has the same theoretical media-key overlap — brightness — but was implemented as directed since the task named it explicitly without a hedge.)
    - `EditorCommand.findReferences`/`.renameSymbol` added and dispatched through the same `AppState.perform` → `postEditorCommand` → `NotificationCenter` → `Coordinator.handle(_:)` path as every other editor command, gated behind the same "text view must be first responder" check as goToLine/toggleComment/etc.
    - `EditorView.Coordinator`: `promptRenameSymbol` mirrors `promptGoToLine`'s exact NSAlert-with-accessory-NSTextField modal pattern, pre-filled with the identifier under the caret (found via a shared top-level `identifierWordRange(in:around:)` helper, also used by ⌘D's "select next occurrence", since it grows in both directions from the caret unlike the backward-only completion-prefix helpers). `requestFindReferences` reads the caret position and hands off to a new `onFindReferences` closure on `EditorView`; `AppState`/`EditorContainerView` do the rest (no UI logic in the Coordinator beyond the prompt itself).
    - **Cross-view navigation refactor:** extracted the position-to-scroll math that used to live as a private `EditorView.Coordinator.scrollToPosition` into a new canonical `EditorScrollProxy.jumpTo(line:column:)` (`Editor/MinimapView.swift`) — now the single implementation shared by Cmd+Click go-to-definition's `navigate(to:)`/`consumePendingDefinitionScroll()` *and* the new cross-view "Find All References panel row click" jump, instead of a second copy. The panel-click case needed a new bridge since it originates outside the editor's own view hierarchy (unlike Cmd+Click): `AppState.pendingNavigationTarget` is set by a new `navigateTo(_:)`/`navigateToReference(_:)`, threaded into `EditorView` as a `pendingNavigationTarget` prop + `onNavigationConsumed` closure, and consumed by a new `Coordinator.consumePendingNavigation(_:currentFileURL:onConsumed:)` from inside `updateNSView` once the target tab's content is loaded — deliberately clearing `AppState`'s side via `Task { onConsumed() }` rather than synchronously, since mutating `@Observable` state synchronously from within `updateNSView` (mid view-update) is undefined behavior in SwiftUI.
    - **Find All References UI:** reused `SearchResult` as the data shape (mapping each `DefinitionLocation` into one by reading the hit file's line content — from the open tab's in-memory content when open, since that's what the LSP server's document view actually is, else from disk, cached per-file since one symbol commonly has multiple references in the same file — and repurposing `matchRange.location` to carry the 1-based column since `SearchResult` has no dedicated column field) rather than inventing a new results model. A new `BottomPanel.references` tab + `ReferencesPanelView.swift` hosts them via new `AppState.referencesResults`/`referencesSymbol`/`isFindingReferences` state, opened automatically (`showBottomPanel = true; activeBottomPanel = .references`) when Find All References runs — kept separate from `appState.searchResults` so a lookup never clobbers an in-progress workspace search.
    - **Rename apply + dirty-file safety (scope decision requested by the task):** chose **apply-on-top-of-the-in-memory-buffer**, not skip-with-warning. Rationale: for any file currently open in a tab, the LSP server's view of that document is whatever `didOpen`/`didChange` last sent it — the tab's in-memory content — not necessarily what's on disk, so rename edits are *always* applied to `openTabs[index].content` regardless of dirty state (applying to on-disk content for a dirty tab would use the wrong base text and could mis-position edits). A clean tab is then written straight to disk immediately (nothing to lose). A dirty tab is deliberately **not** written to disk: the rename lands in the buffer alongside the user's unsaved edits and the tab stays dirty, so one Cmd+S persists both together — skip-with-warning was rejected because it would leave a stale, unrenamed reference sitting in a dirty buffer, silently breaking the very consistency the rename was meant to guarantee. Files not open in any tab are read from and written straight back to disk. `AppState.renameSymbol` reports a summary status message ("Renamed to "X" — N files saved, M applied to unsaved buffer(s) (save to persist)").
    - `AthenaCommands.swift`: added "Find All References" / "Rename Symbol…" to the Go menu for menu-bar discoverability, matching the existing `performAction(...)` wiring pattern.
    - No new tests added: `LSPManager.references`/`.rename` are actor methods requiring a live subprocess language server, same untested-by-design precedent items 10/12 already established for `definition`/`formatting` (no fake-LSP-server harness exists to reuse); the new `AppState` orchestration (`findReferences`, `renameSymbol`, `navigateTo`) has no existing precedent of direct `AppState` unit testing in this codebase to extend either (no constructor-injectable test seam for canned LSP/file-service responses beyond real actors). Verified via `make build-debug` (zero warnings/errors, Swift 6 strict concurrency) and `make test` (44/44 green, same count as before — no regressions).
    - Not independently verified in this pass: on-screen confirmation of the References panel's rendering and the rename prompt's dialog against a *real* running language server in this dev environment (same caveat items 10/12 already noted for their own LSP-dependent features) — verified by code inspection plus the green build/test run.
    - **Post-implementation review pass (same day)** caught and fixed three real bugs and three cleanup items before this landed:
      - *Stale-offset corruption:* `textDocument/rename` is an async round-trip nothing blocks editing during (only the preceding NSAlert blocks, and it's dismissed before the LSP call resolves) — if the user typed into an affected buffer while the rename was in flight, the server's offsets (computed against an older snapshot) would get applied to newer content, silently corrupting it. Fixed by snapshotting every open tab's content *before* the request goes out; any tab whose content no longer matches its snapshot when the response lands has its edits skipped for that file (reported via status message) instead of applied against stale offsets.
      - *LSP desync:* `renameSymbol` was setting `openTabs[index].content` directly without ever calling `lspManager.didChange`, unlike every other content-mutating path (`updateTabContent`) — the server's document model would permanently disagree with the buffer after a rename. Fixed by sending `didChange` for every open-tab file the rename touches.
      - *Dead-end re-click:* `consumePendingNavigation`'s dedup compared the pending target by `DefinitionLocation` value equality, so clicking the *same* reference row twice failed the "already consumed" guard on the second click and did nothing (no scroll, no clear). Fixed by wrapping the target in a new `NavigationRequest` (`SharedTypes.swift`) carrying a fresh `UUID` per `navigateTo(_:)` call; the Coordinator now dedups by request `id`, not location, so a repeat click of the same spot is recognized as a new request and still jumps.
      - *Cleanup:* moved `SearchFileGroup`/`SearchFileGroupView`/`SearchResultRowView` out of `SearchPanelView.swift` into a new `SearchResultComponents.swift` (CLAUDE.md: "One view per file in UI/" — they'd been widened to internal in place rather than relocated); factored the duplicated file-grouping loop into one `SearchFileGroup.grouping(_:)` static, used by both `SearchPanelView` and `ReferencesPanelView`; factored the duplicated identifier/word-boundary scan (`AppState`'s `identifier(inLine:)`/`isWordChar` vs. `EditorView.Coordinator`'s `isIdentifierChar`/`wordRange`) into shared top-level `isIdentifierChar`/`identifierWordRange` functions in `SharedTypes.swift`, used by both; and made `LSPManager.parseWorkspaceEdit`'s `changes` parsing per-entry-tolerant (one malformed file entry no longer drops the entire workspace edit) — the reviewer flagged this as a quick, worthwhile robustness fix rather than a must-fix crash.
      - *Also applied:* `findReferences` now caches each file's content the first time it's read instead of re-reading/re-splitting per reference (a symbol commonly has several references in one file).
      - *Deliberately left as follow-ups, not fixed:* a narrow race where two rapid, *different* References-panel clicks can interleave in `navigateTo` and silently drop the first jump (the end state is still consistent — the second click's jump succeeds — just not both; not trivial to fix without a more invasive in-flight-request-cancellation mechanism, and the reviewer flagged it as low-priority/self-correcting); and the pre-existing stylistic overlap between `renameSymbol`'s dirty-tab handling and `replaceAllInWorkspace`'s (different enough in intent — one must preserve unsaved edits, the other always writes through — that forcing one shared helper felt like the wrong abstraction) plus the `promptRenameSymbol`/`promptGoToLine` NSAlert boilerplate (two call sites, not yet worth a shared modal helper).
      - Re-verified after all fixes: `make build-debug` zero warnings/errors; `make test` 44/44 green (one intervening run showed 3 `FileWatchService` tests failing together under load from concurrent build/review-agent activity — re-ran clean to confirm it was transient, not a regression, since none of these fixes touch `FileWatchService.swift`).
14. **Go to Symbol / outline / breadcrumbs (A8)** — M — LSP `documentSymbol`; ⇧⌘O palette mode reuses command-palette infra; breadcrumbs bar above editor from the same data.
    - **Done (2026-07-05):** `LSPManager.documentSymbols(fileURL:)` sends `textDocument/documentSymbol` and normalizes the response into a new `DocumentSymbol` (`SharedTypes.swift`) tree via `parseDocumentSymbol`, handling both LSP result shapes — hierarchical `DocumentSymbol` (`range`/`selectionRange`/`children?`) and the older flat `SymbolInformation` (`location: {uri, range}`, never nested, whose full range is reused as its selection range so "jump to symbol" still lands somewhere sane). `kind` is kept as the raw LSP `SymbolKind` int (mirroring `completionKindName`'s existing pattern) with a computed `iconName` mapping all 26 kinds to an SF Symbol (letter-square glyphs for class/method/property/etc., matching Xcode's own jump-bar convention). `line`/`character` are the 1-based selection point; `rangeStartLine`/`rangeEndLine` carry the symbol's full extent for breadcrumb containment tests; `children` is `nil` (not empty array) for leaves so `List`/`OutlineGroup`-style consumers don't draw an empty disclosure triangle.
      Two new pure, unit-tested top-level functions in `SharedTypes.swift`: `flattenDocumentSymbols(_:depth:)` (depth-first flatten pairing each symbol with its nesting depth, for the ⇧⌘O palette's filterable-but-still-indented list) and `breadcrumbSymbolPath(in:containingLine:)` (walks the tree, deepest containing match wins at each level — the one piece of this feature with a pure seam to test, same precedent as `applyLSPTextEdits`; `LSPManager.documentSymbols`/`parseDocumentSymbol` itself is untested by design, same actor/live-subprocess precedent as `definition`/`references`/`rename`/`formatting`).
      `AppState.documentSymbols` is the single tree Go to Symbol, the Outline panel, and breadcrumbs all read from — no feature issues its own LSP request. Populated by `loadDocumentSymbols(for:)` (wired via a second `.task(id: tab.id)` on `CodeEditorView`, alongside the existing blame one — refetches on every tab switch, serving `documentSymbolsCache[path]` instantly while the fresh request is in flight) and `scheduleDocumentSymbolsRefresh` (800 ms debounce from `updateTabContent`, matching `EditorView.Coordinator`'s ghost-text/completion debounce style). `closeTab` clears `documentSymbols` when the last tab closes (no `CodeEditorView` left to own the refresh). `AppState.breadcrumbPath` is a computed property over `documentSymbols` + the active tab's existing `cursorLine` — no new cursor-tracking plumbing, since `onCursorMove` already updates `cursorLine` and `@Observable` recomputes the computed property for free.
      `KeyAction.goToSymbol` (⇧⌘O) dispatches through `perform(_:)` → `presentQuickOpen(prefill: "@")`, identical to how `.quickOpen`/`.commandPalette` already open the palette — added to the Go menu too. `QuickOpenView` gained a third mode (`isSymbolMode`, `@` prefix, VS Code parity) alongside the existing file/command modes: filters `flattenDocumentSymbols(appState.documentSymbols)` by contains+prefix-sort (same ranking as the other two modes), renders via a new `SymbolPaletteRow` (icon + name, indented by depth — same 14pt-per-level convention `FileTreeView`/`OutlineSymbolRow` use), and jumps via `AppState.navigateTo(_:)` — the exact `NavigationRequest`/`pendingNavigationTarget`/`EditorScrollProxy.jumpTo` cross-view path Find All References/Rename Symbol already established, not a new jump implementation.
      New `Sources/Athena/UI/OutlineView.swift` (`SidebarPanel.outline`, wired into `ActivityBarView`/`SidebarView` the same way `.database`/`.npm`/`.debug` already are — no dedicated `KeyAction` to show it, matching those three): flat, depth-indented `ScrollView`/`LazyVStack` of `flattenDocumentSymbols` rows (not `List`/`OutlineGroup` — this codebase has no precedent using `List`, every sidebar panel uses the same hand-rolled `ScrollView` + row-view pattern, so this follows that instead of introducing a new one); clicking a row jumps via the same `navigateTo` path.
      Breadcrumbs bar: new private `BreadcrumbBarView` in `EditorContainerView.swift` (this file already hosts several small views — `CodeEditorView`, `ExternalChangeBanner` — so one more thin bar follows precedent rather than needing its own file), inserted above `CodeEditorView` inside the `activeTab != nil` branch. Renders nothing when the path is empty (no symbols yet, or cursor outside every range) rather than an empty bar. Each segment is clickable (not just read-only) and jumps via `navigateTo` — the interactive nicety the task said was optional was included since it reuses the exact same one-liner as the Outline/palette jump.
      **Scope deviations:** none from the task's explicit asks; `documentSymbolsCache`/`blameCache` are both never pruned on tab close (existing `blameCache` precedent — matched, not fixed) other than the one live bug found in self-review below.
      **Self-review catch (same pass):** closing the *last* open tab left `AppState.documentSymbols` holding the closed tab's stale tree (no `CodeEditorView` remains to fire the tab-switch `.task`, and `loadDocumentSymbols`'s early-return path only fires for a *new* tab, not zero tabs) — the Outline panel's own empty-state guard (`activeTab?.fileURL == nil`) masked this in that one view, but the ⇧⌘O palette would still show and let you select the dead tab's symbols. Fixed by clearing `documentSymbols` directly in `closeTab` when `openTabs` becomes empty.
      `make build-debug`: zero warnings/errors under Swift 6 strict concurrency. `make test`: 51/51 green (44 prior + 7 new — 3 `flattenDocumentSymbols` + 4 `breadcrumbSymbolPath`); one intervening run showed the 3 `FileWatchService` tests failing together (untouched by this change) — re-ran clean, confirming the same transient/timing flake already documented for item 13, not a regression.
      Not independently verified in this pass: on-screen confirmation of the palette/Outline/breadcrumbs against a *real* running language server in this dev environment (same caveat every LSP-dependent item in this plan has noted) — verified by code inspection, the green build/test run, and re-using navigation/debounce mechanisms already proven working by items 10/12/13.
15. ✅ **Inline diagnostic squiggles (A2 finish)** — S/M — `AthenaLayoutManager` already does custom drawing (whitespace glyphs); add underline drawing for diagnostic ranges + gutter markers.
    - **Done (2026-07-05):** Went with `NSLayoutManager` temporary attributes (`addTemporaryAttribute`/`removeTemporaryAttribute`), not custom `AthenaLayoutManager` glyph drawing — matched the most recent precedent for "temporary overlay coexisting with the regex highlighter," bracket-pair match highlighting (item 11), which uses the same mechanism for the same reason: `SyntaxHighlighter`'s per-keystroke `textStorage.setAttributedString` invalidates temporary attributes across the edited range, so both overlays reapply from `EditorView.Coordinator.textViewDidChangeSelection` — NSTextView always moves the selection when a character is typed, so that hook gets "reapply after every edit" for free without a second per-keystroke observer. `updateDiagnosticHighlights(in:)` mirrors `updateBracketMatchHighlight`'s clear-then-repaint shape exactly (`highlightedDiagnosticRanges` parallels `highlightedBracketRanges`).
    - `EditorTheme.swift`: added `diagnosticError`/`diagnosticWarning`/`diagnosticInfo` (new hex fields + computed `NSColor` accessors, same raw-hex-storage pattern every other semantic color already uses), themed per built-in (Darcula/One Dark/GitHub Light) rather than hardcoded raw `NSColor` literals in the drawing code.
    - `SharedTypes.swift`: `Diagnostic` and `DiagnosticSeverity` gained `Equatable` — `Diagnostic.==` compares by content (fileURL/line/column/message/severity), deliberately ignoring `id`, since every fresh `publishDiagnostics` batch reconstructs new `Diagnostic` values with new random `id`s even when nothing changed; `EditorView.updateNSView` needs value equality to gate reapplication.
    - `EditorView.swift`: new `diagnostics: [Diagnostic]` parameter (threaded from `CodeEditorView`'s `fileDiagnostics` computed property, keyed by `appState.diagnostics[tab.fileURL]` exactly like `blameInfo`/`breakpoints`). `Diagnostic` carries only a 1-based line/column point from the LSP, not an explicit end range, so `diagnosticRange(for:in:)` underlines the identifier/word touching that point (reusing the shared `identifierWordRange` helper — same one Cmd+Click/⌘D/rename already use) when there is one, falling back to the whole line (minus its line terminator) otherwise. Severity → style: `.error`/`.warning` get a themed dotted underline (`NSUnderlineStyle.single` ∪ `.patternDot`) at full color; `.information` gets a thin plain underline at 50% alpha; `.hint` gets none at all — matching the task's "no underline, or a very subtle one, for `.information`/`.hint`."
    - Hover priority: `triggerHover` now checks `diagnostic(at:in:)` — a local scan of the same `currentDiagnostics`/`diagnosticRange` used for painting, so "hovering the squiggle" can't disagree with where it's actually drawn — before falling through to the existing LSP `onRequestHover` round-trip, showing the diagnostic's message in the same `HoverWindowController` panel when found.
    - `GutterView.swift`: new `diagnostics: [Int: DiagnosticSeverity]` (highest severity per line, `.error`/`.warning` only — reduced by a new `EditorView.gutterDiagnosticSeverities(from:)` static helper, synced the same way `breakpoints`/`debugLine` already are from `updateNSView`), drawn as a small dot at the gutter's left edge (`.systemRed`/`.systemOrange`) — deliberately on the opposite side from the existing breakpoint dot/debug arrow (right edge) so a line can show both without collision.
    - `make build-debug`: zero warnings/errors under Swift 6 strict concurrency (verified via a touch + incremental rebuild of every changed file, full log grepped for "warning"/"error" — no matches). `make test`: 51/51 green; one run hit the already-documented `FileWatchService` timing flake (2 tests, different pair than previously noted — `watchedFileWriteYieldsFileChanged`/`watchedDirectoryEntryYieldsDirectoryChanged`, unrelated to this change), re-ran clean to confirm.
    - Not independently verified in this pass: on-screen confirmation of the squiggle rendering, gutter dots, and hover-priority behavior against a *real* running language server (same caveat every LSP-dependent item in this plan has noted) — verified by code inspection and the green build/test run. No new automated tests added: this is `NSTextView`/`NSLayoutManager`-coupled UI drawing code with no existing unit-test seam in this codebase (same untested-by-design precedent as bracket-pair matching/multi-cursor in item 11).
16. ✅ **Snippets with tab stops (B5)** — M — minimal engine: `$1`, `$2`, `${1:placeholder}`, Tab cycles. Upgrade Drizzle completions to use it; add user snippets JSON later.
    - **Done (2026-07-05):** `Editor/SnippetEngine.swift` (new) — a small, pure, `Sendable` `SnippetEngine.expand(_:)` parsing `$1`/`$2`/… (ordered tab stops), `${1:placeholder}`/`${1}` (default text, empty-braced also supported), and `$0` (final cursor, synthesized at the end of the expanded text when absent, never duplicated when explicit). Returns `ExpandedSnippet` (plain `text` + `tabStops: [SnippetTabStop]`, ordered ascending by stop number with `$0` always last). Malformed input (unterminated `${1:...`, a trailing lone `$`) falls back to literal text rather than crashing or partially consuming. Nested/mirrored placeholders and transforms are explicitly out of scope, per the task.
    - `EditorView.Coordinator.insertCompletion` now expands every accepted completion's `insertText` through `SnippetEngine` before inserting. Zero tab stops (plain text, the common case) or exactly one (only an implicit/explicit `$0`) behaves exactly as before — caret placed at the single relevant point, no tracking. Two or more stops make the snippet "active": a new private `ActiveSnippet` (ordered stops + `currentIndex`) is stored on the Coordinator, and the first stop's range is selected *before* `applyHighlighting` runs (mirroring `wrapSelection`'s existing selection-then-rehighlight ordering so the re-highlight's selection-preserving logic doesn't clobber it) — placeholder text pre-selected so typing overwrites it via AppKit's native replace-selection behavior, no custom interception needed.
    - Tab-cycling wired into `handleKeyDown`'s existing case-48 priority chain, checked *after* completion-accept and ghost-text-accept (both of which are likely exactly what triggers a snippet becoming active in the first place): a new `advanceSnippet(in:)` moves `currentIndex` forward and selects the next stop's range, clearing `activeSnippet` once it lands on the final (`$0`) stop — the Tab keystroke that lands you there is still consumed, not passed through as a literal tab character. Escape (case 53) also cancels an active snippet, in the same priority chain as popup/ghost-text/find-bar/multi-cursor dismissal.
    - A new `NSTextViewDelegate.textView(_:shouldChangeTextIn:replacementString:)` keeps the active snippet's tracked ranges correct as edits land: grows/shrinks the current stop in place when an edit falls inside it, shifts every later stop by the same length delta — the same offset bookkeeping `applyLSPTextEdits` already does for a batch of edits, applied here to one live edit at a time. `textViewDidChangeSelection` cancels the snippet the moment the selection lands outside the (kept-in-sync) current stop's range — covering "clicking elsewhere," an arrow-key move out of the placeholder, or any other native selection change — without needing a special-cased flag, since `advanceSnippet` always updates `currentIndex`/the stored range *before* calling `setSelectedRange`, so its own selection-change notification already reads as "within the new current stop."
    - **Bug caught in self-review before landing:** the editor reuses a single `NSTextView` across tab switches — `EditorView.updateNSView`'s `textView.string != content` branch swaps content via a direct `textView.string =` assignment that deliberately never reaches `shouldChangeTextIn`/`textViewDidChangeSelection` (same reason `applyHighlighting`/`consumePendingDefinitionScroll` are called manually right after it). Without an explicit reset, a snippet accepted in tab A would silently stay "active" after switching to tab B, and a subsequent Tab press could call `setSelectedRange` with a stale range from a completely different document — at best a no-op, at worst out-of-bounds. Fixed with a new `Coordinator.cancelActiveSnippet()`, called from that same content-swap branch alongside the other per-tab-switch resets.
    - `Services/DrizzleCompletionService.swift`: authored real tab-stop snippets for every completion with an actual fill-in-the-blank shape — query-builder methods (`from(${1:table})`, `where(${1:condition})`, join helpers, `values`/`set` object literals, `transaction`/`pgTable`/`sqliteTable` block bodies with a stop inside), all comparison/logical operators (`eq(${1:column}, ${2:value})`, etc.), column-type definitions (`varchar('${1:name}', { length: ${2:255} })` — the task's own example — `decimal`, `pgEnum`, `foreignKey`, …), and relation helpers (`relations`/`one`/`many`). Left alone: completions with no meaningful argument (`notNull()`, `defaultNow()`, `onConflictDoNothing()`, bare `count()`/`returning()`).
    - `Services/LSPManager.swift`: declared `"completionItem": ["snippetSupport": true]` in the `initialize` request's `textDocument.completion` capabilities — `parseCompletions` already threaded a completion item's raw `insertText` straight through unmodified (so LSP `insertTextFormat: Snippet` responses were already reaching `insertCompletion` verbatim), but servers commonly only *emit* snippet-shaped `insertText` once the client declares this capability; without it many servers silently downgrade to plain-text inserts even for parameterized completions (function calls, etc.).
    - New `@Suite("SnippetEngine")` (10 tests): the task's own worked example (`"foo(${1:a}, $2)$0"` → text + ordered stops incl. `$0`), numeric ordering independent of source order, placeholder-text capture, empty-placeholder `${1}` shorthand, explicit-`$0`-not-duplicated, implicit-`$0`-defaults-to-end, a lone trailing `$` and an unterminated `${1:abc` both falling back to literal text without crashing, and multi-digit stop numbers (`${10:...}` vs `$2`) sorting correctly. `make build-debug`: zero warnings/errors under Swift 6 strict concurrency. `make test`: 61/61 green (51 prior + 10 new); one run hit the already-documented `FileWatchService` timing flake (2 of 4 tests, a different pair than previously noted), re-ran clean twice to confirm it's the known flake, not a regression.
    - Not independently verified in this pass: on-screen confirmation of Tab-cycling/placeholder-selection against the running app (same caveat several earlier LSP/UI-adjacent items in this plan have already noted) — verified by code inspection, the green build/test run, and by tracing the exact selection/edit event sequence (`insertCompletion` → `setSelectedRange` → `textViewDidChangeSelection` → `updateActiveSnippetTracking`; a keystroke inside a stop → `shouldChangeTextIn` → `textDidChange`) against this codebase's existing, already-proven bracket-pair-matching and multi-cursor mechanisms, which rely on the same notification ordering.
17. ✅ **Line ops (B7)** — S — move/duplicate/delete line keybindings; pure text manipulation, quick win alongside B1.
    - **Done (2026-07-05):** Five new `KeyAction` cases (`moveLineUp`/`moveLineDown`/`copyLineUp`/`copyLineDown`/`deleteLine`, category "Editor") in `Models/KeyBinding.swift`, bound to VS Code's exact macOS defaults — ⌥↑/⌥↓, ⇧⌥↑/⇧⌥↓, ⌘⇧K — none collided with an existing binding (no prior binding used the up/down/k keys). Each forwards through `AppState.perform` → a new `EditorCommand` case (`Models/SharedTypes.swift`) → `postEditorCommand`, handled in `EditorView.swift`'s `Coordinator.handle(_:)` alongside `toggleComment`/`indent`/`outdent` (same "requires the text view to be first responder" gate).
    - **Decision — no separate "Duplicate Line" binding:** VS Code has no distinct default keybinding for "duplicate line" beyond Copy Line Down (`editor.action.copyLinesDownAction`); implemented Move Up/Down + Copy Up/Down only, per the task's own fallback guidance.
    - **Decision — Copy Line Up/Down cursor semantics** (the task flagged this as ambiguous): verified against actual VS Code muscle-memory behavior — repeatedly pressing Copy Line Up stacks duplicates *upward* while the caret stays trailing at the bottom of the stack (on the original content, now pushed down); repeatedly pressing Copy Line Down stacks duplicates *downward* while the caret leads at the newest copy. Implemented exactly that: Copy Line Up's selection lands on the original (shifted down by the inserted copy's length); Copy Line Down's selection lands on the new copy.
    - Core logic factored as pure, `NSTextView`-independent functions in new `Editor/LineOperations.swift`: `enum LineOperations` with `moveUp`/`moveDown` (return `nil` — a no-op — at the top/bottom of the document) and `copyUp`/`copyDown`/`deleteLines` (always succeed), all taking `(text: String, selection: NSRange)` and returning an `Edit` (minimal replacement range + replacement text + post-edit selection), mirroring `applyLSPTextEdits`'s "pure function operating on `NSString`/`NSRange`" style. Every function operates on `(text as NSString).lineRange(for: selection)` — the same primitive `toggleComment`/`shiftIndent` already use — so a multi-line selection moves/copies/deletes every touched line, not just the caret's line.
    - Edge cases handled directly in the range arithmetic, no special-casing: last line with no trailing newline (a moved/copied line gains or loses its terminator exactly when its "is this the last line" status changes); empty file; single-line file; multi-line selections. Verified by hand-tracing `NSString.lineRange(for:)`'s actual boundary behavior (via a scratch script) rather than assuming it, since a trailing newline creates a distinguishable empty final "line" position in Foundation's model.
    - `EditorView.swift`'s `Coordinator` gained a new `replace(_:with:in:selecting:)` overload of the existing undo-aware `replace(_:with:in:selectWhole:)` helper (`shouldChangeText`/`replaceCharacters`/`didChangeText`, never raw `textStorage` mutation) — for callers with an exact post-edit selection to apply rather than "select the whole replacement" — plus a small `applyLineEdit(_:in:)` that calls it for each of the five new commands.
    - New test suites in `Tests/AthenaTests/AthenaTests.swift`: `LineOperations — Move Line Up/Down` (7 tests), `LineOperations — Copy Line Up/Down` (6 tests), `LineOperations — Delete Line` (5 tests) — 18 tests total, covering ordinary swaps, top/bottom no-ops, last-line-without-trailing-newline terminator handling, multi-line selections, single-line and empty files. `make build-debug`: zero warnings/errors under Swift 6 strict concurrency (full clean rebuild, log grepped for "warning"/"error" — no matches). `make test`: 79/79 green (61 prior + 18 new); the already-documented `FileWatchService` timing flake (3 of 4 tests, a different subset each of 3 repeated runs) reproduced consistently across three separate `make test` invocations, unrelated to this change (nothing in `FileWatchService.swift` was touched) — confirms it's the known flake, not a regression.
    - Not independently verified in this pass: on-screen confirmation of the five new shortcuts against the running app (same caveat several earlier UI-adjacent items in this plan have already noted) — verified by code inspection, the pure-function test suite, and by tracing the exact dispatch path (`AppState.dispatchKeyInfo` → `perform` → `postEditorCommand` → `Coordinator.handle`) against the already-proven `toggleComment`/`indent`/`outdent` commands, which share the identical NotificationCenter plumbing and `replace` primitive.
    - **This completes Phase 2 — Editing power** (items 11–17 all done).

### Phase 3 — Workbench: git depth, splits, terminals (3–4 weeks)

18. ✅ **Diff viewer (D2)** — L — side-by-side scrolling-synced read-only editors with change highlighting; entry points: git panel file click, gutter indicator click. Smallest slice: unified inline diff rendered with existing highlighter.
    - **Done (2026-07-06, scoped to the smallest slice):** shipped the **unified (single-pane) inline diff**, not the side-by-side split-pane view — per the task's own "smallest slice" guidance, side-by-side scrolling-synced editors are left for a future pass.
    - `Editor/UnifiedDiffParser.swift` (new): pure, `Sendable` parser — `UnifiedDiffParser.parse(_:)` turns `git diff`/`git diff --cached` text into `ParsedDiff` (`hunks: [DiffHunk]`, `isBinary: Bool`). Detects `"Binary files ... differ"` up front and returns `isBinary: true` with no hunks rather than attempting to parse it as text. Each `DiffHunk` carries its raw `@@ -a,b +c,d @@` header plus parsed `oldStart`/`oldCount`/`newStart`/`newCount`, and an ordered `[DiffLine]` (`kind: .context/.added/.removed`, 1-based `oldLineNumber`/`newLineNumber` — `nil` on the side that doesn't apply — and `text` with the leading marker byte stripped). Metadata lines before the first hunk (`diff --git`, `index`, `---`, `+++`) and `"\ No newline at end of file"` markers are skipped, not misparsed as content.
    - Untracked files (point 4): `git diff` has nothing to show them against `HEAD`, so `ParsedDiff.wholeFileAsAdded(_:)` (same file, a pure factory) synthesizes a single all-`.added` hunk from the file's current on-disk content instead of calling the parser on empty diff text — reuses the exact same hunk-rendering UI rather than a separate "untracked" view mode.
    - `EditorTheme.swift`: added `diffAdded`/`diffRemoved` (new hex fields + init params + computed `NSColor` accessors), following the exact same raw-hex-storage pattern as `diagnosticError`/`diagnosticWarning` from item 15 — themed per built-in (Darcula/One Dark/GitHub Light), not hardcoded literals in the drawing code.
    - `AppState.swift`: new "Diff Viewer" state block — `diffViewerChange: GitFileChange?` (nil = not presented), `diffViewerStaged: Bool`, `diffViewerParsedDiff: ParsedDiff`, `diffViewerIsLoading`/`diffViewerErrorMessage`, and a computed `showDiffViewer` (mirrors `breadcrumbPath`'s "derive visibility from state" pattern rather than a second bool that could drift out of sync). `openDiffViewer(for:staged:)` branches on `change.status == "??"` (untracked, per `GitService.status`'s own convention) to read the file directly and use `wholeFileAsAdded`, otherwise calls `gitService.diff(path:staged:at:)` and parses the result; `closeDiffViewer()` clears the change. `closeFolder()` also clears `diffViewerChange` so a stale overlay can't survive a workspace switch.
    - **UI placement decision:** `UI/DiffViewerView.swift` (new) is a full-window dimmed-backdrop overlay — reusing `QuickOpenView`'s existing `ZStack` + `Color.black.opacity(0.45)` + rounded card pattern verbatim (`MainWindowView` gained a second `.overlay { if appState.showDiffViewer { DiffViewerView() } }` alongside the existing Quick Open one) rather than a new sheet/modal mechanism or a panel that replaces the editor area — smallest architectural footprint for this single-window app, just sized larger (920×640) than the command palette since diff content needs room. Escape and a close button both dismiss; tapping the dimmed backdrop also dismisses, matching Quick Open.
    - Each line's code content is run through the real `SyntaxHighlighter` (same language detection via `Language.detect(from:)`, same `currentTheme`) and rebuilt as a concatenation of colored `Text` fragments (`NSAttributedString.enumerateAttribute(.foregroundColor:)` → `Text(substring).foregroundColor(...)` joined with `+`) rather than a scoped `NSAttributedString` → `AttributedString` conversion — sidesteps `AttributeScopes` entirely while still giving real per-token syntax color, not plain monospace text. `+`/`-`/` ` gutter markers and old/new line-number columns sit to the left; added/removed rows get a `diffAdded`/`diffRemoved`-tinted background at 15% opacity so syntax color stays legible on top.
    - `GitPanelView.swift`: clicking anywhere on a file row (`GitFileRow`'s `.onTapGesture`) now opens the diff viewer for that row's change/staged-ness; the existing "Open File" context-menu action is untouched (still opens the real editor tab) and a new "View Diff" context-menu item was added alongside it for discoverability. Stage/unstage button and discard-confirmation alert both continue to work unchanged — `Button`-inside-tappable-row already had precedent in this codebase (`TabBarView`'s close button inside its tab-click row), so nesting a plain `onTapGesture` around the existing hover-revealed stage/unstage `Button` was safe to add without special-casing hit-testing.
    - New `@Suite("UnifiedDiffParser")` (9 tests): mixed context/added/removed classification with correct old/new line numbers, multiple hunks parsed independently, additions-only and deletions-only hunks (new/deleted files), binary-file detection, `"\ No newline at end of file"` markers not counted as content, and three `wholeFileAsAdded` cases (normal multi-line content, trailing-newline artifact dropped, empty content yields no hunks). `make build-debug`: zero warnings/errors under Swift 6 strict concurrency (full clean rebuild). `make test`: 88/88 green (79 prior + 9 new); no `FileWatchService` flake observed in this run.
    - **Known limitation, not fixed here:** `FileService.readFile`'s ISO-Latin-1 fallback can successfully "decode" arbitrary binary bytes as garbled text, so an *untracked* binary file (e.g. an image) currently renders as garbled all-green "added" lines rather than a binary placeholder — only *tracked* binary files (where git itself reports `"Binary files ... differ"`) get the proper placeholder. A content-sniffing heuristic (e.g. null-byte detection) would close this gap; left as a follow-up since it's a pre-existing `FileService` limitation, not new to this feature.
    - Not independently verified in this pass: on-screen confirmation of the overlay's rendering/interaction against the running app (same caveat several earlier UI-adjacent items in this plan have already noted for this sandbox) — verified by code inspection, the green build/test run, and reuse of `QuickOpenView`'s already-proven overlay/dismissal mechanism.
19. ✅ **Git gutter indicators (D4)** — S/M — `git diff` per open file (debounced on save) → colored bars in `GutterView` (added/modified/deleted).
    - **Done (2026-07-06):** reused the item-18 parser end to end rather than writing a second one. `Editor/UnifiedDiffParser.swift` gained `GitLineChangeType` (`.added`/`.modified`/`.deleted`) and a pure `UnifiedDiffParser.classifyLineChanges(in: ParsedDiff) -> [Int: GitLineChangeType]`, keyed by 1-based new-file line number. Each hunk is walked in contiguous non-context runs: an all-added run → `.added`; a run mixing removed+added pairs them 1:1 as `.modified` (any excess *added* line beyond the pairing is `.added`; an excess *removed* line in a mixed run is deliberately left unmarked — see below); an all-removed run → a single `.deleted` marker attached to the next kept line (or, at the end of a hunk, one past the last known new-file line) rather than needing a line of its own, per the task's "boundary marker, not its own line" guidance.
    - **Scope decision (collision avoidance):** a naive per-pair implementation risked writing `.deleted` onto a line a *later* run would then overwrite with `.added`/`.modified` (or vice versa) when add/remove counts didn't match. Fixed by only ever emitting `.deleted` for a run with zero added lines — by construction (one contiguous non-context run is collected as a single unit) that run's anchor line can only be a kept context line or hunk-end, never a line another run also classifies. Documented inline in `classifyLineChanges`'s doc comment; covered by a dedicated test (`excessRemovedLinesInAMixedBlockAreNotSeparatelyMarkedDeleted`).
    - `EditorTheme.swift`: added `diffModified` (new hex field/init param/computed `NSColor` accessor, same raw-hex-storage pattern as `diffAdded`/`diffRemoved`) — went with the 3-color scheme (green/blue/red) over reusing `diffAdded` for both added and modified, since VS Code's own gutter draws these differently and the distinction is one extra field. Picked a blue accent per theme (Darcula `0x3592C4`, One Dark `0x528BFF` — matching its existing cursor blue, GitHub Light `0x0366D6`).
    - `GutterView.swift`: new `gitLineChanges: [Int: GitLineChangeType]` + `theme: EditorTheme` properties (the view previously had no theme reference at all — item 15's diagnostic dots used hardcoded `NSColor.systemRed`/`.systemOrange` rather than the theme, so this is the first themed gutter element). Change bar renders as a 3pt-wide colored strip along the gutter's absolute left edge (x: 0–3) spanning the line's full height for `.added`/`.modified`; `.deleted` instead draws a small triangular notch at the row's top-left corner (4pt legs) rather than a full-height bar, matching the "thin marker at the boundary" convention the task described. Shifted the existing diagnostic dot's x-position from `diagDotRadius + 2` to `changeBarWidth + diagDotRadius + 1` so the two left-edge indicators (change bar and diagnostic dot) sit adjacent without overlapping — the only change to item 15's existing code, made necessary by adding a second left-edge element.
    - `AppState.swift`: new `gitLineChanges: [URL: [Int: GitLineChangeType]]` (mirrors `diagnostics: [URL: [Diagnostic]]`'s per-file keying exactly). `refreshGitLineChanges(for tab:)` mirrors `loadBlame(for:)`'s shape/guard (only runs when a `.git` directory exists at the workspace root); a private `computeGitLineChanges(for fileURL:)` does the actual `gitService.diff(path: fileURL.path, staged: false, at:)` → `UnifiedDiffParser.parse` → `classifyLineChanges` pipeline (passing the file's absolute path straight through, the same way `GitBlameService.blame` already does — git accepts absolute paths regardless of the process's cwd, so no relative-path computation was needed), with an untracked-file branch reusing `ParsedDiff.wholeFileAsAdded` (same helper the item-18 diff viewer uses for the identical "nothing to diff against HEAD" situation) so a new, not-yet-tracked file still shows a full green bar instead of nothing. Returns `nil` (leave existing state alone) on any failure/missing-workspace/no-`.git` case, distinct from "no changes" (`[:]`).
    - Debounce: `scheduleGitLineChangesRefresh(tabId:fileURL:)` copies `scheduleDocumentSymbolsRefresh`'s exact 800 ms cancel-and-reschedule shape (same `Task.sleep`/cancellation/`activeTabId == tabId` staleness guard), called from `updateTabContent` alongside the existing document-symbols debounce — no per-keystroke `git diff` subprocess calls.
    - Recompute triggers wired exactly as scoped: tab switch (`CodeEditorView`'s `.task(id: tab.id) { await appState.refreshGitLineChanges(for: tab) }`, alongside the existing blame/document-symbols tasks), after save (`saveActiveTab`, `saveActiveTabAs` — including clearing the old URL's stale entry when the path changes — and `saveAllTabs`, one `await refreshGitLineChanges(for:)` per successfully-written tab), and the debounced post-edit refresh above. `closeTab`/`closeFolder` clear the now-irrelevant cache entries, mirroring `diagnostics`' own cleanup.
    - `EditorContainerView.swift`/`CodeEditorView`: new `fileGitLineChanges` computed property (`appState.gitLineChanges[tab.fileURL] ?? [:]`, same shape as `fileDiagnostics`), threaded into `EditorView` as a new `gitLineChanges` parameter.
    - `EditorView.swift`: new `gitLineChanges: [Int: GitLineChangeType]` parameter; `Coordinator.currentGitLineChanges` gates re-sync to the gutter (mirrors `currentDiagnostics`'s change-gated pattern) in both `makeNSView` and `updateNSView`; the gutter's `theme` is now set on creation and refreshed alongside every other theme-driven update in the `themeChanged` branch.
    - New `@Suite("UnifiedDiffParser.classifyLineChanges")` (8 tests): pure addition, one paired replacement, excess-added-beyond-pairing, excess-removed-in-a-mixed-run-not-marked-deleted (locks in the collision-avoidance decision above), pure removal anchored to the next kept line, pure removal at hunk end anchored one-past-the-last-line, multiple hunks combining into one result, and `wholeFileAsAdded` classifying every line `.added`. `make build-debug`: zero warnings/errors under Swift 6 strict concurrency (verified on a full clean rebuild). `make test`: 96/96 green across the new count (88 prior + 8 new); the already-documented `FileWatchService` full-suite-only timing flake (plan.md item 6) reproduced on `watchedFileWriteYieldsFileChanged` in 3 consecutive `make test` runs, confirmed unrelated by running `swift test --filter FileWatchServiceTests` in isolation (4/4 green) — same pre-existing, investigated, not-a-regression flake every recent item in this plan has hit.
    - Not independently verified in this pass: on-screen confirmation of the change bar/deletion notch rendering against the running app (same caveat several earlier UI-adjacent items in this plan have already noted for this sandbox) — verified by code inspection, the green build/test run, and the pure-function test suite covering every classification branch.
20. ✅ **Branch switcher + clone + history (D3, D6)** — M — status-bar branch menu (list/checkout/create — service methods exist); implement the Welcome screen's Clone Repository; commit-history list using existing `log`.
    - **Done (2026-07-06):**
    - `GitService.branches(at:)` now returns `[GitBranch]` (`name`/`isCurrent`/`isRemote`, new `SharedTypes.swift` type) instead of a lossy `[String]` — the old version stripped the `"remotes/"` prefix without recording that a branch *was* remote, which made local/remote impossible to tell apart downstream. Parsing was pulled out into a pure `static func parseBranches(_:)` (an actor's `static` members aren't actor-isolated, so it's directly callable from tests with no `await`/mock process). Also drops the remote's symbolic `HEAD -> origin/main` pointer line and a detached-HEAD `"(HEAD detached at …)"` marker, neither of which is a real checkable-out branch. New `@Suite("GitService.parseBranches")` (7 tests) covers all of the above against real `git branch -a` output shapes.
    - `StatusBarView.swift`: the branch label is now a `Menu` (`.menuStyle(.borderlessButton)`, no existing Picker/popover precedent in this codebase to match, so plain SwiftUI `Menu` was the smallest fit) listing local branches (current one checked via `Label(_, systemImage: "checkmark")` and disabled) with remote-tracking branches grouped under a "Remote Branches" submenu, plus a "Create New Branch…" entry at the top using the same `NSAlert`-with-accessory-`NSTextField` pattern as `EditorView.Coordinator.promptGoToLine`/`promptRenameSymbol`. Selecting a branch calls new `AppState.checkoutBranch(_:)`; creating one calls new `AppState.createAndCheckoutBranch(named:)` (which just calls `GitService.createBranch` — already `git checkout -b` in one step). Both refresh via `refreshGitStatus()`, which now also opportunistically refreshes `AppState.branches` (best-effort — a branch-listing failure doesn't clobber a successful status refresh's `statusMessage`).
    - `UI/CommitHistoryView.swift` (new): read-only commit list backed by new `AppState.commitHistory`/`isLoadingCommitHistory`/`commitHistoryErrorMessage` + `refreshCommitHistory(limit:)`, which calls the already-implemented (but previously unused by any UI) `GitService.log(at:limit:)`. Each row shows message/short-hash/author/date; clicking one calls new `AppState.openDiffViewer(forCommit:)`. Surfaced into `GitPanelView` via a History/Changes toggle button in the existing header (smallest integration — swaps the panel's body content, not a new sidebar tab) rather than a new top-level `SidebarPanel` case.
    - Commit diffs reuse the item-18 diff viewer end to end rather than a second renderer: added `GitService.diff(commit:at:)` (`git show <hash>`; its leading commit-message header is harmless because `UnifiedDiffParser.parse` already ignores every line before the first `@@` hunk header) and a parallel `AppState.diffViewerCommit: GitCommit?` alongside the existing `diffViewerChange`, mutually exclusive with it. `DiffViewerView`'s header now shows the commit's message/short-hash/author when `diffViewerCommit` is set instead of a file name/path; since a commit can span multiple files and `ParsedDiff` doesn't track a file path per hunk, syntax highlighting falls back to `.plaintext` in this mode rather than guessing from one file.
    - `GitService.clone(url:into:)` (new): runs `git clone <url> <folder-name>` with the working directory set to `destination`'s *parent* — the one method in this actor where `at:` isn't an existing repo directory, since `destination` itself must not already exist. Folder-name derivation (`GitService.repoFolderName(from:)`, e.g. `"https://github.com/user/repo.git"` → `"repo"`, also handling scp-style `git@host:repo.git` SSH URLs and a trailing slash) is a separate pure `static func`, covered by a new `@Suite("GitService.repoFolderName")` (6 tests).
    - `WelcomeView.swift`: "Clone Repository" now prompts for a URL (`NSAlert`-with-textfield, same pattern as above) then a destination folder (`NSOpenPanel`, matching `openFolder()`'s existing folder-picker exactly), then calls new `AppState.cloneRepository(urlString:destinationParent:)`, which computes the full destination path, runs the clone, and — on success — calls `appState.openWorkspace(_:)` on the result. No live progress bar (out of scope per the task); "Cloning…"/failure/success states surface via `appState.statusMessage`, which is always on-screen (the status bar sits outside the Welcome-vs-editor content swap in `MainWindowView`).
    - `make build-debug`: zero warnings/errors, full clean rebuild. `make test`: 109/109 green (96 prior + 13 new: 7 branch-parsing + 6 repo-folder-name); the already-documented `FileWatchService` full-suite-only timing flake (plan.md item 6) reproduced on `watchedFileWriteYieldsFileChanged` across 3 consecutive `make test` runs, confirmed unrelated to this change by running `swift test --filter FileWatchServiceTests` in isolation (4/4 green, 0.6s) — same pre-existing, investigated, not-a-regression flake prior items in this plan have hit.
    - Not independently verified in this pass: on-screen confirmation of the branch menu / commit history panel / clone flow against the running app (same sandbox screen-recording/Accessibility permission caveat noted by earlier UI-adjacent items in this plan) — verified by code inspection and the green build/test run.
21. ✅ **Multiple terminals (E1)** — M — tab strip inside the terminal panel; `TerminalView` already encapsulates one SwiftTerm instance; add split later.
    - **Done (2026-07-06):** `TerminalSession` (new `SharedTypes.swift` type — `id`/`title`/`shell`, `Identifiable`/`Sendable`) models one terminal tab. `AppState` gained `terminalSessions: [TerminalSession]`/`activeTerminalSessionId: UUID?` mirroring `openTabs`/`activeTabId` exactly, plus `newTerminalSession()`/`closeTerminalSession(_:)`/`activateTerminalSession(_:)` mirroring `openNewTab()`/`closeTab(_:)`/`activateTab(_:)`. `closeTerminalSession`'s adjacent-selection logic is factored into a pure `TerminalSession.nextActiveId(afterClosing:in:previousActiveId:)` (mirrors `closeTab`'s "prefer the session now at the same index, falling back to the last one" rule) — the one clean pure-function seam in this otherwise UI-heavy feature, covered by a new `@Suite("TerminalSession.nextActiveId")` (6 tests: non-active close, active-close-prefers-same-index, active-close-of-last-falls-back, closing-the-only-session, unknown id, nil-stays-nil).
    - Titling: first session created each launch is just the shell name (`"zsh"`, from the existing `$SHELL` detection), subsequent ones numbered (`"zsh 2"`, `"zsh 3"`, …) off a monotonically increasing `AppState.terminalSessionSequence` counter (not `terminalSessions.count`, so titles stay unique even after closing a middle session and opening a new one). `AppState.init` calls `newTerminalSession()` once so exactly one session exists at launch — matching the pre-existing single-terminal behavior, no empty state forced on users who never touch this feature.
    - `TerminalView.swift`: `TerminalView` now wraps one `LocalProcessTerminalView` bound to a `TerminalSession` (`session.shell` instead of reading `$SHELL` directly) and takes an `isActive` flag that calls `nsView.window?.makeFirstResponder(nsView)` in `updateNSView` when it becomes the visible session, so typing reaches the shell immediately after switching tabs. Added `static func dismantleNSView` — investigated `LocalProcess`'s actual lifecycle (`.build/checkouts/SwiftTerm/Sources/SwiftTerm/LocalProcess.swift`) and confirmed its `deinit` only cancels the exit-event monitor, it never signals the child process, so without an explicit `nsView.terminate()` on dismantle, every closed terminal tab would leak its shell as an orphaned background process for the app's remaining lifetime. `dismantleNSView` also nils `nsView.processDelegate` *before* calling `terminate()` so the resulting exit event can't reach `Coordinator.processTerminated` and auto-restart a shell for a tab that's already gone (the existing auto-restart-on-exit behavior is otherwise preserved unchanged for a shell that exits on its own, e.g. the user typing `exit`).
    - New `Sources/Athena/UI/TerminalTabStripView.swift`: `TerminalTabStripView` + private `TerminalTabItemView`, a smaller sibling of `TabBarView`/`TabItemView` (same close-button-on-hover-or-active, "+"-button, active-tab underline chrome) without dirty-dot tracking or drag-reorder (out of scope per the task; terminal tabs have no "unsaved" concept).
    - `BottomPanelView.swift`: new private `TerminalPanelView` (the Terminal case's content) composes the tab strip above a `ZStack` of **every** open session's `TerminalView`, toggling only `.opacity`/`.allowsHitTesting` on the active one rather than conditionally instantiating — confirmed via the `LocalProcess` investigation above that conditionally removing/reinstantiating a session's view would kill and respawn its shell on every tab switch, which this avoids entirely; background sessions keep running untouched while hidden. Closing every session shows a small "No terminal sessions / New Terminal" empty state (mirrors the editor's zero-open-tabs → Welcome-screen pattern) rather than a permanently-disabled close button on the last tab.
    - `make build-debug`: zero warnings/errors on a full clean rebuild under Swift 6 strict concurrency. `make test`: 115/115 green on one run (109 prior + 6 new); the already-documented `FileWatchService` full-suite-only timing flake (plan.md item 6) reproduced on `watchedFileWriteYieldsFileChanged` across 3 subsequent `make test` runs, confirmed unrelated to this change (nothing in `FileWatchService.swift` was touched) by running `swift test --filter FileWatchServiceTests` in isolation (4/4 green, 0.6s) — same pre-existing, investigated, not-a-regression flake every recent item in this plan has hit.
    - Not independently verified in this pass: on-screen confirmation of the terminal tab strip / multi-session switching against the running app (same sandbox screen-recording/Accessibility permission caveat noted by earlier UI-adjacent items in this plan) — verified by code inspection, the green build/test run, and tracing the exact SwiftTerm process lifecycle (`terminate()` → SIGTERM → `childMonitor` exit event → `processTerminated`) against the library's own source rather than assuming it.
22. ✅ **Split editors (C2)** — L — two editor groups side-by-side first (not arbitrary grids); tab model becomes per-group. Do after tabs gain drag-reorder (C5, S) since both touch `TabBarView`/tab state.
    - **Done (2026-07-06), scoped down exactly as this task instructed:** tab drag-reorder (C5) was never actually scheduled as its own item in this plan (an oversight the task called out), so this shipped without it — no dependency on drag-and-drop. Exactly 2 groups (primary/left, secondary/right), side-by-side only — no vertical split, no recursive/nested splitting, no arbitrary N-way grid.
    - **Model:** `AppState.openTabs`/`activeTabId` ARE the primary group's state, unchanged and untouched in shape — new `AppState.secondaryGroup: EditorGroup?` (new `SharedTypes.swift` type: `id`/`tabs`/`activeTabId`) is `nil` for single-pane (today's behavior) and only exists once "Split Editor Right" runs. New `AppState.focusedGroup: EditorGroupSide` (`.primary`/`.secondary`, new `SharedTypes.swift` enum with an `.other` helper) tracks which pane the user last interacted with — always `.primary` while unsplit, so every "current file" command's group-aware branch is a no-op fork in single-pane mode.
    - **Confirmed identical single-pane behavior:** every rewritten method (`openFile`, `closeTab`, `activateTab`, `updateTabContent`, `saveActiveTab`/`saveTab`, `saveActiveTabAs`, `revertActiveTab`, `saveAllTabs`, `openNewTab`, `cycleTab`, `discardChanges`, `replaceAllInWorkspace`, the external-file-change handlers) routes through new `tabs(in:)`/`setTabs(in:)`/`activeTabId(in:)`/`setActiveTabId(in:)` helpers that are literally `openTabs`/`activeTabId` for `.primary` — same storage, same code path, when `secondaryGroup == nil`. `make test` (123/123, see below) exercises the pre-existing single-pane suites unchanged.
    - **Verified the plan's own "mostly just works" claim before changing anything:** grepped every `openTabs`/`activeTabId`/`activeTab` reference in `AppState.swift` and every UI file first. Confirmed `diagnostics`/`gitLineChanges`/`blameCache`/`debugBreakpoints` are genuinely URL/path-keyed dictionaries with zero reference to "the active tab," so `loadBlame(for:)`/`refreshGitLineChanges(for:)` needed **no changes** — both panes' `CodeEditorView` instances call them independently and correctly for their own tab. `documentSymbols`/`breadcrumbPath`, however, turned out to be a single global tree (not per-file), so letting both panes' `.task(id: tab.id)` drive it independently would have raced — fixed by centralizing the fetch into ONE `.task(id:)` in `EditorContainerView`, keyed on `(focusedGroup, focusedTab?.id)` (refetches on a focus change between panes even with no tab-id change, not just on tab switch).
    - **Group-aware (per the task's own explicit list):** Cmd+S/Save As/Revert (`focusedTab`-scoped via a new shared `saveTab(id:in:)`), format-on-save (inherits automatically — `formattedContent(for:)` already took a plain `TabModel`), the breadcrumbs bar + Outline panel + Go to Symbol (`OutlineView.swift`, `QuickOpenView.swift` now read `appState.focusedTab`), cursor-position tracking (new `AppState.setCursorPosition(tabId:in:line:column:)`, called from each pane's own `onCursorMove` — this is also what marks a pane "focused" the moment you click into it or move its caret). Status bar Ln/Col, ChatView's "include current file" context, and ClaudePanel's slash-command context were also switched from `activeTab` to `focusedTab` for consistency (identical result when unsplit, more correct when split).
    - **Explicit, defensible scope-limits (not bugs, documented in code comments at each site):** opening a file via the file tree/Quick Open/Go to Definition/Find References navigation/session restore/debugger-pause-navigation all funnel through `openFile(_:)`, which reveals an already-open file wherever it is or else opens into the *focused* group — but session restore itself only ever persists/restores the **primary** group (a relaunch always starts single-pane; split layout doesn't survive a relaunch). `renameSymbol`/`findReferences`'s "prefer this file's in-memory buffer over disk" optimization still only checks primary-group tabs — a file open **only** in the secondary pane with unsaved edits falls back to a disk read/write for those two features specifically (narrow edge case: split + rename/find-references + unsaved secondary-only file).
    - **Correctness fix found and closed while wiring this up (would otherwise have been a real regression the moment a split existed):** `EditorView.Coordinator.handle` lets `.find`/`.findAndReplace` bypass the `firstResponder` check (deliberately, so ⌘F reaches the editor even when focus is elsewhere in the window) — with two `EditorView`/`Coordinator` instances alive simultaneously, both would have received the same `.athenaEditorCommand` notification and popped open their own find bar at once. Fixed with a new `EditorView.isFocusedGroup` flag (`appState.focusedGroup == side`, always `true` unsplit) gating just those two cases.
    - **Splitting a file:** "Split Editor Right" (View menu + `⌘\`, VS Code's real default, verified no existing binding claimed `\`) creates `secondaryGroup` on first use and copies the focused tab's file into it as an **independent** `TabModel` — its own id/cursor/scroll position/dirty flag, matching VS Code (not a shared tab instance across groups). `AppState.splitEditorRight()`; repeat presses on the same file re-activate the existing secondary copy instead of piling up duplicates. Closing the secondary group's last tab collapses it back to `nil` (single-pane) automatically inside `closeTab`. `closeTab` also only tears down a closed tab's LSP/file-watch/diagnostics/gutter state once the file isn't *also* still open as the other group's independent tab on the same path.
    - **UI:** `Sources/Athena/UI/EditorContainerView.swift` — new private `EditorPaneView(side:)` (tab bar + active editor or, primary-only, `WelcomeView`) and `SplitEditorPanesView` (a `GeometryReader`-driven `HStack` with a 4pt draggable `SplitterHandleView` — clamped 20%–80%, not pixel-perfect resizable, per the task's own "simple drag handle is a reasonable middle ground" framing). One shared breadcrumb bar sits above either layout, reflecting `focusedTab` rather than being duplicated per-pane (smallest footprint, same "smallest slice" precedent as item 18's diff viewer). `Sources/Athena/UI/TabBarView.swift` gained a `side: EditorGroupSide = .primary` parameter (defaults preserve every existing call site) — its own "+" button and "Close Others"/"Close All" now scope to that side's tabs only.
    - New `@Suite("TabModel.nextActiveId")` (6 tests, mirroring the existing `TerminalSession.nextActiveId` suite exactly — the one clean pure-function seam this refactor fell out onto, shared by both groups' close-tab logic) and `@Suite("EditorGroupSide")` (2 tests, the `.other` helper). `make build-debug`: zero warnings/errors on a full clean rebuild under Swift 6 strict concurrency. `make test`: 123/123 green (115 prior + 8 new); the already-documented `FileWatchService` full-suite-only timing flake (plan.md item 6) reproduced on `watchedFileWriteYieldsFileChanged` in one of several `make test` runs, confirmed unrelated (nothing in `FileWatchService.swift` was touched) by running it in isolation via `swift test --filter FileWatchServiceTests` (4/4 green) — same pre-existing, investigated, not-a-regression flake every recent item in this plan has hit.
    - Not independently verified in this pass: on-screen confirmation of the split-pane layout/splitter drag/focus-switching against the running app — attempted via the built debug binary in this sandbox (`./.build/debug/Athena`, launched and stayed alive with no crash/error output), but `screencapture` failed with "could not create image from display" confirming no display session is available here (same sandbox limitation every recent UI-adjacent item in this plan has already noted) — verified instead by code inspection, the green build/test run, and reasoning through the exact SwiftUI re-render/task-firing sequence for both the single-pane and split cases.
23. ✅ **Merge-conflict UI (D5)** — M — detect conflict markers, render Accept Current/Incoming/Both buttons above each hunk.
    - **Done (2026-07-06). This completes ALL of Phase 3.**
    - `Editor/ConflictParser.swift` (new): pure parser, no `NSTextView`/AppKit dependency — `ConflictRegion` (`oursLabel`/`theirsLabel`, `oursText`/`theirsText`, `startLine`/`endLine`, and three `NSRange`s: `range` spanning all three marker lines terminator-inclusive, plus `oursRange`/`theirsRange` for just each side's content) and `ConflictParser.parse(_:) -> [ConflictRegion]`, which walks the text line-by-line via repeated `NSString.lineRange(for:)` calls (the same idiom `GutterView`/`EditorView.Coordinator` already use) rather than a naive `"\n"`-split, so ranges are byte-accurate and safe to hand straight to `NSTextView.replaceCharacters(in:with:)`. Supports multiple regions per file; an unmatched marker (no following `=======`/`>>>>>>>`) is skipped, not misparsed. `ConflictResolutionChoice` (`.ours`/`.theirs`/`.both`) + `ConflictRegion.resolvedText(for:)` compute the replacement text (`.both` omits either side if it's empty, so accepting both when one side deleted the block doesn't leave a stray blank line). `ConflictRegion.compareDiff` synthesizes a `ParsedDiff` (ours as `.removed`/old, theirs as `.added`/new) so the gutter menu's "Compare" action reuses `DiffViewerView` (item 18) verbatim instead of a second diff renderer — exactly the "clean reuse" the task allowed for, and it turned out to be one.
    - **Conflict detection / false-positive gate:** `GitService.status(at:)`'s porcelain parsing was pulled out into a pure `static func parsePorcelainStatus(_:)` (mirrors `parseBranches`/`repoFolderName`), which now also classifies unmerged `XY` codes (`DD`/`AU`/`UD`/`UA`/`DU`/`AA`/`UU` — either side `"U"`, or both sides matching `"A"`/`"D"`) into a new `GitStatus.conflicted: [GitFileChange]` bucket instead of the old parsing's behavior of stuffing a `"UU"` line into *both* `staged` and `unstaged`. `AppState.isConflictScanTrusted(for:)` gates the editor's raw marker-text scan against this: trusts the scan alone when there's no workspace or `gitStatus` hasn't loaded yet (ambiguous default, errs toward showing the UI); once a real status is in hand, only a file git itself lists as unmerged opens the gate — any other known state (clean/modified/untracked/staged) closes it, since real conflict markers are themselves an uncommitted change git would have flagged as unmerged. This is the guard against a file that merely *contains* the literal marker strings for an unrelated reason (e.g. documentation about git).
    - **UI treatment:** `EditorView.Coordinator.updateConflictHighlights` mirrors `updateBracketMatchHighlight`/`updateDiagnosticHighlights`'s exact clear-then-repaint temporary-attribute shape, called from the same three spots (initial load, a fresh file swapped into the same `NSTextView`, and `textViewDidChangeSelection` — typing always moves the caret, so conflict regions stay in sync after every keystroke the same "for free" way those two get). Paints `oursRange`/`theirsRange` with `diffRemoved`/`diffAdded` at 15% opacity — treating a conflict like an old/new diff (ours = losing/old, theirs = incoming/new) rather than adding two new `EditorTheme` colors, which the task explicitly allowed and reads consistently with the Compare view's own old/new coloring.
    - **Action UI — went with the gutter-menu option** (of the three offered) as "achievable without a large custom NSView overlay system": `GutterView` gained `conflictRegions: [ConflictRegion]`, drawing a small orange diamond at each region's `startLine` only. Clicking anywhere on one of a region's lines (not just the marker line) pops a plain `NSMenu` (Accept Ours / Accept Theirs / Accept Both / a separator / Compare Ours ↔ Theirs) instead of toggling a breakpoint. Each "Accept …" action calls `EditorView.Coordinator.resolveConflict(_:choice:)`, which replaces the ENTIRE region (`region.range`, all three marker lines included) through the exact same undo-aware `replace(_:with:in:)` path every other editor command uses — a normal, undoable edit. "Compare" calls a new `AppState.openDiffViewer(forConflict:in:)`, which reuses `diffViewerChange`/`diffViewerParsedDiff` (setting a placeholder `GitFileChange` so `DiffViewerView`'s existing filename/path header and per-file language detection come along for free) rather than adding a third mutually-exclusive diff-viewer-source field.
    - Point 4 ("stop showing the UI once resolved") needed no separate bookkeeping: resolving a region is a normal edit, which fires `textDidChange`/`textViewDidChangeSelection` same as any keystroke, which re-runs `updateConflictHighlights` against the post-edit text — a file with zero remaining markers simply parses to zero regions.
    - `GitPanelView.swift`: also surfaced `gitStatus.conflicted` as a new "Merge Conflicts" section (reusing `ChangeSection`/`GitFileRow` verbatim), shown first — ahead of Staged/Unstaged/Untracked — matching VS Code's own ordering, since the old parsing silently dropped conflicted files into a confusing staged-and-unstaged double-listing.
    - New `@Suite("ConflictParser")` (10 tests: no-markers, single region content/label extraction, exact range boundaries including the trailing-newline-inclusive convention, multiple independent regions, an empty-sided region, two "unmatched marker" malformed cases, `resolvedText` for all three choices including the empty-side-omitted case, and `compareDiff`'s removed/added mapping) and `@Suite("GitService.parsePorcelainStatus")` (8 tests covering every documented unmerged code, the "AA vs A " non-collision case, and that a conflicted line no longer double-lands in staged/unstaged). `make build-debug`: zero warnings/errors, full rebuild. `make test`: 141/141 green (123 prior + 18 new) on three consecutive runs except the already-documented `FileWatchService` full-suite-only contention flake (plan.md item 6) reproducing on `watchedFileWriteYieldsFileChanged` in all three runs, confirmed unrelated (nothing in `FileWatchService.swift` was touched) by `swift test --filter FileWatchServiceTests` in isolation (4/4 green) — same pre-existing, investigated, not-a-regression flake every recent item in this plan has hit.
    - Not independently verified in this pass: on-screen confirmation of the gutter marker/menu and background tint rendering against the running app (same sandbox screen-recording/Accessibility permission caveat noted by every recent UI-adjacent item in this plan) — verified by code inspection, the green build/test run, and reuse of already-proven mechanisms (the bracket-match/diagnostic overlay pattern, `DiffViewerView`, and the undo-aware `replace` path).

### Phase 4 — Debug depth & polish (2–3 weeks, opportunistic)

24. ✅ **Debug: watch expressions + REPL input + conditional breakpoints (E2, E3)** — M — DAP `evaluate` and `setBreakpoints` with conditions are protocol-supported already; add UI in `DebugSidebarView` + input row in Output.
    - **Done (2026-07-06), scoped exactly as tasked: watch expressions + REPL console (E2's evaluate half, E3). Conditional breakpoints (E2's other half) were not part of this pass's instructions and remain plain-breakpoints-only** — left for a follow-up (the `setBreakpoints` DAP request already accepts a `condition` per-breakpoint; only the UI to type one and the CDP `Debugger.setBreakpointByUrl`'s own `condition` param are still missing).
    - **`DebugService.evaluate(expression:frameId:context:)`** (new, alongside the existing `continueExecution`/`stepOver`/`fetchStackFrames`/`fetchVariables` controls): DAP path sends the standard `evaluate` request (`{expression, frameId, context}` → `{result, type, variablesReference}`) exactly like `fetchVariables`'s `scopes`/`variables` calls. CDP path uses `Debugger.evaluateOnCallFrame` (via the paused frame's `callFrameId`, looked up from the same `cdpFrames` array `fetchVariables`/`fetchStackFrames` already read) when a frame is selected, else `Runtime.evaluate` — and checks the response's `exceptionDetails` first, surfacing the exception's `description`/`text` as a thrown `DAPError.requestFailed` instead of returning a bogus "result". Three pure, file-scope-testable static parsing helpers pulled out the same way `GitService.parsePorcelainStatus` was: `parseDAPEvaluateResponse`, `parseCDPEvaluateResponse` (prefers `description`, falls back to raw `value`, else `"undefined"` — matches `fetchVariables`'s existing CDP display-string convention), `cdpEvaluateExceptionMessage`.
    - **Selected stack frame** (didn't exist before — call-stack rows weren't clickable): new `AppState.selectedFrameId: Int?`, defaulted to the top/innermost frame every time `refreshDebugState()` runs (the same private method the DAP/CDP `stopped`-event callback already called for the stack-frame/variables refresh — watch/REPL evaluation hooks into that exact same trigger point per the task). `DebugSidebarView`'s call-stack rows are now tappable (`AppState.selectStackFrame(_:)`, refetches variables for that frame + re-evaluates watches) and show a selection tint independent of the "topmost frame" arrow glyph, so you can inspect an outer frame without losing sight of where execution actually paused.
    - **Watch expressions**: new `SharedTypes.swift` model `WatchExpression` (`expression`, `lastValue`/`lastType`/`lastError`, `Sendable`) plus three pure list-mutation helpers (`appending`/`removing`/`applying`, mirroring `TabModel.nextActiveId`'s "extract the seam" convention) that `AppState.addWatchExpression(_:)`/`removeWatchExpression(_:)`/`reevaluateWatchExpressions()` delegate to — the actual `AppState` methods are thin wiring, the list logic itself needs no live `DebugService`/debug session to test. `reevaluateWatchExpressions()` evaluates every expression against `selectedFrameId` with `context: "watch"`; a failing expression (out-of-scope variable, syntax error, whatever) lands its error on that row via `WatchEvaluationOutcome.failure` (a plain enum, not `Result<_, String>` — `String` isn't `Error`, and the failure case is just a display message) — never thrown further, never touches `statusMessage`. Expressions persist across debug sessions (only their last-evaluated value clears in `stopDebugging()`); a fresh pause re-evaluates all of them.
    - **UI**: `DebugSidebarView` gained a "WATCH" section (text field + Return-to-add, between Variables and Breakpoints) and a private `WatchExpressionRowView` whose hover-reveal `xmark` remove button mirrors `TabItemView`/`TerminalTabItemView`'s exact close-button convention (14×14pt circle, `Color.primary.opacity` hover fill, shown only on hover).
    - **REPL console**: new bottom-panel tab — `BottomPanel.debugConsole` (new case, wired into `BottomPanelView`'s tab row + content switch) → new `Sources/Athena/UI/DebugConsoleView.swift`, a scrollback transcript (`AppState.debugConsoleEntries: [DebugConsoleEntry]`) plus a bottom text input. Enter calls `AppState.evaluateInConsole(_:)`, which evaluates via the same `DebugService.evaluate` with `context: "repl"` against `selectedFrameId`, appending expression+result (or error, styled red) to the transcript. Input field is disabled (greyed, placeholder text changes) whenever `DebugState` isn't `.paused` — `evaluateInConsole` also guards on `.paused` itself as a belt-and-suspenders check against a stale Enter keypress racing a resume.
    - New `@Suite("DebugService evaluate parsing")` (9 tests: DAP response with all/missing/no fields, CDP response preferring `description` over raw `value`, falling back to raw `value`, and `"undefined"` with neither, CDP exception-message extraction — none/exception-description/text-fallback) and `@Suite("WatchExpression")` (7 tests: trim-and-append, blank-expression no-op, remove-by-id, remove-unknown-id no-op, success clears prior error, failure clears prior value, untouched-when-no-matching-result). `make build-debug`: zero warnings/errors on a full clean rebuild under Swift 6 strict concurrency. `make test`: 157/157 green (141 prior + 16 new) on one run; two subsequent runs reproduced the already-documented `FileWatchService` full-suite-only contention flake (plan.md item 6) on `watchedFileWriteYieldsFileChanged` specifically, confirmed unrelated (nothing in `FileWatchService.swift` was touched) by `swift test --filter FileWatchServiceTests` in isolation (4/4 green, 0.6s) — same pre-existing, investigated, not-a-regression flake every recent item in this plan has hit.
    - Not independently verified in this pass: on-screen confirmation of the Watch section/call-stack row selection/Debug Console tab against a live paused debug session (would need a real DAP/CDP adapter attached and a running app window) — same sandbox display-session limitation noted by every recent UI-adjacent item in this plan (`screencapture`/no display server here) — verified instead by code inspection, the green build/test run, and reuse of already-proven mechanisms (`fetchVariables`'s CDP display-string convention, `TabItemView`'s hover-close-button pattern, `refreshDebugState`'s existing pause-trigger wiring).
25. **Hover-to-inspect while paused (E4)** — S/M — reuse hover panel from Phase 1 with DAP `evaluate` in the selected frame.
25. **Hover-to-inspect while paused (E4)** — S/M — reuse hover panel from Phase 1 with DAP `evaluate` in the selected frame.
26. **Markdown + image preview (C7)** — S/M — WKWebView-rendered markdown split preview; NSImage view for image files (currently they open as garbled text).
27. **VS Code theme import (C9)** — M — map VS Code theme JSON token colors onto `EditorTheme`; instantly hundreds of themes with zero design work.
28. **Sticky scroll, zen mode (B8, C8)** — M — nice-to-haves once symbols (A8) exist.

### Dependency notes

- A1 (LSP lifecycle) unblocks A2→A9 and item 12 — do first, everything intelligence-related hangs off it.
- Command palette infra (item 5) is reused by ⇧⌘O (14) — build the palette as a generic mode-based picker.
- Diagnostics stream (item 2) before squiggles (15).
- Tab drag-reorder (C5) before split editors (22).
- No new dependencies required for any Phase 0–2 item; Phase 3–4 also fits AppKit/Foundation. Keep the dep list at GRDB + SwiftTerm.

### Suggested order of attack (first two weeks)

1. D1 discard-changes fix (hours — it's a data-integrity bug)
2. A1 LSP lifecycle for TS/JS → then all configured servers
3. A2 diagnostics → Problems panel live
4. C4 file watcher (stale-buffer data loss)
5. B3 auto-indent on newline
6. C1 command palette
